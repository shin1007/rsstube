# いまどうなっているか

**このファイルは報告だけ。** やってほしいことは `docs/owner-todo.md` にあります。

最終更新: 2026-08-31（毎朝のまとめは NotebookLM に決定・記事の遷移を先読みに・本体がPDFの記事）

---

## ひと目でわかる状態

| | |
|---|---|
| コード | P0〜P5 まで実装済み。plan.md に未実装は無い |
| 本番 | https://rsstube.vercel.app で自動運転中（pg_cron 5本） |
| DB | 手元と本番は**同じ Supabase**。`.env.local` の向き先がそのまま本番 |
| マイグレーション | `0034` まで適用済み |
| テスト | 271件（27ファイル） |
| データ | フィード12本 / 購読中の記事359件（本文313件・日付欠け0件）/ ダイジェスト14本 / 音声4本 |

**動いていないもの**: 通知（登録端末0件）、Google Drive。理由と対処は owner-todo.md に。

**Drive は「未接続」ではなく「未設定」でした**（8/27 判明）。オーナーが接続ボタンを
押したところ「Google の認証情報が設定されていません」と出ました。入っているのは
手元の `.env.local` だけだったためです。**認証情報は環境変数から設定画面へ移しました**
（`0033`）。Vercel を触る必要はもうありません。戻り先はいま開いている URL から
組み立てるので、手元と本番で食い違うこともありません。手順は owner-todo.md の「1」。

---

## 毎朝のまとめは NotebookLM（8/31 決定）

**自動でやるのは「選抜 → Markdown → `/exports` に置く → 通知」まで。**
音声にするのは人が NotebookLM でやります。**毎朝の自前音声は自動生成しません**
（作れば毎日 `gemini-3.5-flash` の1日20回と Storage を使いますが、聴くのは
NotebookLM のほうなので無駄になります）。自前音声そのものは残っていて、
記事1本や手で選んだまとめを「音声にする」で作れます——位置づけが
「毎朝のもの」から「押したときだけの深掘り」に変わりました。

いまの朝の手数は、`/exports` の行を押して開くダイアログから:
**.md をダウンロード** か **本文をコピー** → NotebookLM に貼る → **指示文をコピー**して
「音声概要をカスタマイズ」に貼る、の3手です。**Drive を繋げば2手減ります**
（「ソースを追加 → ドライブ → 選ぶ」だけになる）。繋がったら、朝のダイジェストを
そのまま Drive に置く経路を足せます——やるかどうかは owner-todo の「3」で伺っています。

---

## 自動で回っているもの

pg_cron から叩いています（`supabase/scheduler.sql`）。
**直近2日ぶんの実行記録を見て、5本とも失敗ゼロ**でした（worker は576回・poll と digest は48回ずつ）。

| ジョブ | 間隔 | 何をするか |
|---|---|---|
| poll | 毎時 07分 | 購読中の12本を巡回（`0029` 以降、購読していないフィードは対象外）。301 の移転を追い、失敗が続けばサイトから探し直す |
| worker | 5分ごと | 本文抽出 → 要約 → 台本 → 音声合成。45秒で切り上げる |
| digest | 毎時 50分 | 設定時刻（6時）を過ぎていて当日ぶんが無ければ作る |
| purge | 毎日 3:30 | 古い記事本文とジョブの掃除 |
| media-purge | 毎日 3:45 | 14日を過ぎた音声を Storage ごと削除（`0031` で30日から短縮） |

ダイジェストは 8/23・8/24・8/25 と、毎日 06:50（日本時間）に途切れず作られています。

---

## 確かめたこと / 確かめていないこと

### 確かめた

- ダイジェストの自動生成（毎日途切れずできている。直近14本）
- 音声の生成一式（台本 → セグメント分割 → 合成 → MP3 → Storage → 署名URL → 非公開）
- Web Push の鍵・暗号化・ヘッダの中身（**配送以外は全部**）
- フィードの移転追随（NHK が実際に移転していて自動で付け替わった）
- 壊れたフィードの自動復旧（URLを404に差し替えて、自力で戻ることを確認）
- 購読解除で印つき記事が残ること（100件中94件だけ消え、6件が残る）
- バックアップと**復元**（全テーブルを空にして戻し、861件が読める状態まで）
- Google の認証情報（client_id / secret が有効、redirect_uri が登録済み）
- スマホからのログイン（オーナーが実機で成功）
- **音声の詰みからの復帰**（本番のデータで、落とす → 積み直す → 二度押ししても増えない、まで実際に流した）
- **スマホでの記事の遷移**（8/31。前後のボタンが出ること・待ちが無いことをオーナーが実機で確認）
- **本番でのPDF読み取り**（`unpdf` が Vercel でも動く。厚労省の議事録から27,647字）

### 確かめた（8/27 追加）

- **生成した音声の品質** — オーナーが聴いて「いい感じ」。**ここが分かっていなかった
  最後の大きな穴**でした（今までは波形上で無音でないことしか言えなかった）
- **詰まっていた1本の作り直し** — オーナーが「↻ 作り直す」を押し、残り1セグメントから
  再開して完成。8/26 に入れた復帰の経路が、実データで通ったことになります

### 確かめていない

- **通知が実機に届くか** — 端末の登録が要る（`push_subscriptions` は今も0件）
- **スマホでの見え方、PWA のインストール**
- **NotebookLM で実際に鳴らすこと** — ブラウザ操作なのでオーナーの手が要ります。
  **8/31 に「毎朝のまとめは NotebookLM で聴く」と決まった**ので、ここが P2 の
  最後の受け入れテストです
- **複数スライドでの台本生成** — 8/17 に無料枠を使い切って以来、一度も再挑戦できていない
  （下の「詰んでいた」がその理由。いまは押し直せます）
- **Google Drive への実書き込み** — 同意がまだ

---

## 8/26 に直したこと — 音声化が詰んでいた

**音声1本が `synthesizing`（合成中）のまま8日間止まっていました。**
5セグメント中4つまでできていて、最後の1つが Gemini の無料枠切れ（429）で
5回試して諦めた状態です。

壊れ方は「静かに壊れる」型でした。

| どこが | どうなっていたか |
|---|---|
| ワーカー | ジョブが諦めても、`media` の行を誰も `failed` に落とさなかった |
| 「聴く」 | ready でも failed でもない行を「生成中」とみなすので、**できあがり予定時刻を永久に出し続けていた** |
| 再生画面 | 同じ理由で「生成中」。失敗だと分かる場所がどこにも無かった |
| 「音声にする」 | 一意索引に当たるので、押し直しても「既に作ってあります」しか返らず**二度と作れない** |

直したもの（`0027_media_recovery.sql` と、それを呼ぶ側）:

- **見捨てられた音声を落とす** — 生きたジョブ（順番待ち・実行中）が1つも無くなった
  ものだけを `failed` にします。ワーカーが毎回、時間予算の外で必ず通ります。
  「何分経ったら」で判定しないのは、再試行のバックオフ（最大12時間）と食い違って
  まだ生きているものを殺すため
- **作り直せるようにした** — 「聴く」と再生画面に「↻ 作り直す」を出しました。
  **できているところは作り直しません。**上の1本なら、残り1セグメントだけをやり直します
  （台本づくりは1日20回しか叩けないので、全部やり直すと詰まった原因の枠切れを
  もう一度踏むことになる）
- **詰まっていた1本は 8/27 に直りました。**オーナーが「↻ 作り直す」を押し、
  残り1セグメントだけをやり直して完成しています

---

## 8/15〜8/18 に入れたもの（前回の報告以降）

- 音声化がジョブを積めずに落ちていたのを修正（#58）
- シークバー、一覧から直接再生、下部プレイヤーがページを移っても残る（#59 #67 #71）
- スライドを「スライドらしく」し、記事の絵を敷く（#60、`0025`）
- 元記事へのリンクを一覧と再生ページに（#61）
- 台本とスライドの中身が薄いのを改善（#62 #63）
- **ボタンの失敗が黙って消えるのを全体で塞いだ**（#64 #65、`lib/actions/result.ts`）
- 順番待ちが自動で進むようにし、できあがり時刻を出す（#68、`0026`）
- 合成が長引いても関数ごと落ちないようにした（#70）
- 最初から選ばれている記事も既読にする（#72）

---

## 8/27 に入れたもの

- **一覧の無限スクロール。** 先頭60件で頭打ちになり「絞り込むか検索してください」と
  出るだけだったのを、下端が近づいたら60件ずつ継ぎ足す形にした。3000件で打ち止め
  （それより古いものを掘るのは `/library` の仕事）。
  - 記事を開いても読み込んだぶんは消えない。1ページ目はサーバー、続きはクライアントの
    state で持ち、id で重複を落として重ねている
  - 61件目から先の記事を開いても「前の記事 / 次の記事」が出るようにした
    （id だけを引く `listArticleIds`）。スマホには一覧へ戻る以外の導線が無いので、
    ここが欠けると61件目から先は毎回戻ることになる
- **「全既読」の取り消し帯を10秒に伸ばした**（8秒から）。無限スクロールで対象が
  「読み込んだぶん全部」になり、押し間違いの被害が増えたため。
- **Drive が未設定のとき、押す前にそう出すようにした。** それまでは未設定でも
  「接続する」ボタンが出ていて、押すと同意画面にも行かずに設定画面へ戻るだけでした
  （理由は戻ってきて初めて出る）。いまはボタンを押せない見た目にして、
  何をどこに入れるかを書いてあります（8/27 の3本目で、入力欄そのものを
  設定画面に置きました）。
- **キーボードショートカットを、ヘルプを開かなくても見えるようにした。**
  一覧の下端に `j/k 移動` `o 開く` `m 既読` `s ★` `l あとで` を常設し、
  「全既読」ボタンの隣に `⇧A` を出す。残りは今までどおり `?` に畳んである
  （ショートカット自体は前からあったが、`?` を押さないと存在に気づけなかった）。

---

## 8/27 の2本目に入れたもの

- **音声の既定を変えた**（`0031`）。話し方は「1人の語り」、保存は14日（30日から）。
  既にある設定行も寄せてある（**列の default を変えても在る行は変わらない**ので、
  マイグレーションで update している）。
- **声を設定画面から選べるようにした**（`0032`）。それまでは環境変数
  `GEMINI_TTS_VOICE_A` / `_B` で、**オーナーがデプロイし直さないと変えられなかった**。
  好みの話なのでユーザーごとに持たせる。声も media に焼くので、途中で設定を変えても
  作成中の音声の前半と後半で声が変わることはない。
- **既定値を1か所にまとめた**（`src/lib/settings/defaults.ts`）。同じ数字が
  「画面の defaultValue」「保存時のフォールバック」「ワーカーの `??`」「purge の定数」の
  4か所に散っていて、1か所直しても他が古いままになる形だった。
- **サイドバーから空のフォルダを消した。** 購読しているフィードが1本も無い
  フォルダは出さない。フォルダは購読より先に作れるし、最後の1本を解除しても残るので、
  見出しだけの行が残って「押しても記事ゼロの一覧が開く」状態になっていた。
  **未読の数では隠さない**——読み終わったことを根拠に消すと、あとで検索したいときに
  そのフィードへ辿り着けなくなる（「すべて」で開く手段はサイドバーしか無い）。
  いま選んでいるフォルダは空でも残す。設定画面のフォルダ一覧は管理する場所なので
  そのまま（空のフォルダを隠すと消せなくなる）。

---

## 8/27 の3本目に入れたもの — Google の認証情報を環境変数から画面へ

- **`app_config` を作った**（`0033`）。アプリ全体で1行だけの設定
  （`id boolean primary key check (id)` で2行目を DB に拒ませている）。
  **RLS ポリシーは1つも作っていない**ので、Secret キーからしか読めない。
- **OAuth クライアントはアプリに1つ。**ユーザーごとに持つのは同意の結果
  （`google_accounts` の refresh_token）だけなので、`settings` ではなく
  アプリ設定として持たせた。2人目は「接続」を押すだけで済む。
- **戻り先（redirect_uri）の設定を無くした。**いま開いている URL から組み立てる。
  以前は環境変数で、手元と本番で別々に要る値だったため、`.env.local` の localhost を
  本番へ写すと同意画面の先で弾かれた（**こちらには何も戻ってこない**）。
- **シークレットは画面へ返さない。**入っているかどうかだけを出し、空欄の保存は
  「変えない」の意味にする。空文字を書くと、保存ボタンを押しただけで接続が壊れる。
- **バックアップには入れない**（`scripts/db-tables.mjs`）。`google_accounts` と同じで、
  秘密そのものを `backups/` に平文で置くほうが割に合わない。失っても
  Google Cloud Console から取り直して入れ直せる。

---

## 8/31 に入れたもの — 遷移の速さと、本体が別にある記事

### 記事から記事への移動が 290ms → 40ms になりました

**オーナーの実機で「問題なし」を確認済み。** 1回の遷移が `/` の作り直し
（RSC 89KB・転送26KB・Supabase に6往復）で、中身のほとんどは変わっていない
サイドバーと一覧60件でした。そこに3つ重なっていました。

- `revalidatePath('/')` は Server Action の応答に**ページ全体を積んで返す**。
  開いた拍子の既読がこれを連れていたので、遷移と同じ重さの描き直しがもう1回走っていた
- `MarkReadOnView` が同じ記事にもう1通 `markRead` を投げていた
- **`router.prefetch()` は動的ルートでは何もしない**（既定の `kind:'auto'` では
  要求そのものが飛ばない）。効いているつもりで、先読みは一度も動いていなかった

同じ手順を stash で戻して比較した実測（各3回）: 一覧から開く 469→303〜425ms、
次の記事 269〜329ms → **35〜79ms（往復ゼロ）**。通信量は変わっていません
（4遷移で166KB→160KB）。押したあとの往復を、読んでいる間に移しただけです。

### 「前の記事／次の記事」が出ないことがあったのを直しました

帯は前後のリンクがあるときだけ描かれるので、**一覧に居ない記事では帯ごと消えて**
いました。未読ビューでは毎回これが起きます（開く→既読→同じ描画の一覧からは抜けている）。
PCは一覧が横に見えているので気づきにくく、**スマホでは1本目を開いた瞬間から
読み進める手が無くなっていました**。390×844 で本番と同じコードを立てて再現を確認し、
id で見つからないときは `published_at` で「居たはずの場所」を出すようにしました。

### 本体がPDF・別ページの記事を読めるようにしました

表題とリンクしか無いHTML（官公庁・自治体に多い形）で、PDFや別ページを本文にします。
厚労省「歯科医療費の動向」の本体PDFは35ページ・54,605字。記事URLが直接PDFのときも
読みます（以前は「HTMLではない」で捨てていました）。**本番でも動くことを確認済み**
（`unpdf` を足したので、`jsdom` の前科を踏まえて Vercel で実際に叩きました）。

厄介だったのは、**失敗ではなく成功に見えていた**ことです。入口ページでは Readability が
フッターを本文として返し（「〒100-8916 …Copyright ©…」の118字）、閾値を超えるので
抽出成功として保存されていました。健康診断にも出てきません。

辿った先が本体でないほうが怖い（読んでも気づけない嘘の要約になる）ので、
別サイトは追わない・候補が4つ以上あるページは何も追わない・`index.html` は本体にしない・
200字以上増えないなら辿らなかったことにする、で臆病に倒しています。実データ18本で、
本文があるページの挙動が変わらないことを確認しました。

**既にある記事は取り直していません**（50件が薄いまま。うち厚労省20件）。
流すかどうかは owner-todo.md の「8」に置いてあります。

## 8/29 に入れたもの — 読みやすさとログイン

### 文字を2段階大きくしました

- **目盛りのほうを動かしました**（`globals.css` の `@theme`）。`text-xs` が165か所、
  `text-sm` が91か所あり、クラスを1つずつ書き替えると必ず塗り残しが出るためです。
  `text-xs` は 12px → 16px、`text-sm` は 14px → 18px、`text-base` は 16px → 20px。
- 2xl から上は既定の刻みが急に粗くなる（1.5→1.875→2.25rem）ので、そのまま2段階送らず、
  小さいほうと同じ比率（約1.25倍）に留めました。見出しだけが伸びて折り返すのを避けるためです。
- **スマホの入力欄が縮まないようにしました。** 「iOSの自動ズーム避け」で入力欄に
  16px を実寸で書いてあり、そのままだと**大きくする変更が、いちばん打ちにくい画面でだけ
  効かない**ことになっていました。`max(16px, 1em)` の下限に変えてあります。

### 上の帯が縦に伸びていたのを直しました（8/29 の2本目）

文字を大きくした副作用で、**記事の上の操作の帯が画面の1/4を占めていました**。
6つのボタンが1行に入らなくなり、flex が縮めにきて `音声にする` が
1文字ずつ縦に折り返っていたためです（報告の写真のとおり）。

- **狭い画面では記号だけにします。** `←` `★` `◷` `▤` `♪` `↗`。
  PCではこれまでどおり `★ スター` `NotebookLM へ` と出ます。
  読み上げには記号ではなく、いつも同じ言葉（`aria-label`）を渡しています
- 書き出しの記号は、はじめ上向きの矢印（`⤴`）にしていましたが、**16px だと隣の `♪` と
  見分けが付きませんでした**（44px まで大きくすれば別物だと分かる、という程度の差）。
  小さく並べる記号は、線の向きではなく形そのものが違うものを選ぶ必要があります
- どのボタンにも `whitespace-nowrap` と `shrink-0` を付けました。
  **これが無いと、次に何か足したときに同じ壊れかたに戻ります**
- 帯の文字だけ1段階小さく（`text-xs` = 16px）しています。読む字ではなく操作の枠なので
- 375px 幅で実測して **41px・横あふれ無し**（もとは写真のとおり300px近くありました）。
  記号にして空いたぶんは、上下の余白ではなく**指の当たり判定**（`px-3`）に回しています

### 画面の下に「前の記事 / 次の記事」を置きました

- 高さ41px。本文の末尾にあった同じ導線は消しました。**同じものを2か所に出さない**ためと、
  末尾のものは長い記事だとそこへ辿り着くのが仕事になっていたためです
  （読み終える前に次へ行きたいときには、無いのと同じでした）
- **`fixed` にはしていません。** プレイヤーが出ているときは `body` の padding が全体を
  持ち上げてくれるので（`Playback.tsx`）、流れの中に置くだけでプレイヤーの上に乗ります
- 行き先が無い側は押せない見た目にして、場所は空けたままにします。端に来たときに
  ボタンの位置がずれると、隣を押してしまうためです
- 本文の下の余白を96pxから40pxに詰めました。あれは下部タブを避けるためのもので、
  記事を開いている間はそのタブが消えています

### 本文が15pxのまま取り残されていました

`text-[15px]` のように**実寸を直書き**した箇所は `@theme` が届きません。
一番長く見る記事本文がこれで、大きくしたつもりが**本文だけ元のまま**でした。
本文 15→19px、補助的な札 11→14px・10→13px に上げてあります（23か所）。

### スマホで、横に払うと前後の記事へ移れます

- 本文の上で左へ払うと次の記事、右へ払うと前の記事。指の下に行き先を出し、
  70px を越えたら移ります。それまでは戻ります
- **コードや表の中から始めた指は奪いません**（その中を横に見るために要るため）
- **iOS の画面左端からのスワイプは奪えません。** Safari の「戻る」が優先されるので、
  前の記事へは端ではなく真ん中あたりから払ってください
- 一覧の行のスワイプ（左=既読 / 右=あとで）とは別の意味です。場所が違う
  （行の上か、本文の上か）ので取り違えようがなく、本文の上で既読を切り替えたい
  場面もありません

### パスワードを目で確かめられるようにしました

- ログイン画面とパスワード設定画面の入力欄に「見る / 隠す」を付けました
  （`components/PasswordField.tsx`）。
- **どの画面も伏せた状態で開きます。** 一時は「新しいパスワードを決める画面だけは
  はじめから見せる」ようにしていましたが、**パスワード欄が最初から読める画面は世の中に無く、
  見慣れない形はそれだけで「安全でない」と読まれます**（8/31 に戻しました）。
  打ち間違いは確認欄が受け止めます。見せるかどうかは押した人が決めます。

### パスキーでログインできるようにしました（`0034`）

- 指紋・顔・端末のPINで入れます。**パスワードは残します**——端末を替えたときや
  生体認証が通らないときに戻る先が要るためです。
- **登録は設定画面**（「パスキー」の欄）から。ログイン中にしかできません。
  ログイン画面には「パスキーでログイン」のボタンが出ます（対応していない
  ブラウザでは何も出しません）。
- **メールアドレスは打たせません。** 端末に「このサイトの鍵」を持たせる形
  （residentKey）で登録するので、選ぶだけで入れます。打たせたうえで
  「そのアドレスに鍵があるか」を返すと、アドレスの当たり判定にも使われます。
- **Supabase の Auth に WebAuthn はありません**（2026-08 時点）。署名の検証は
  こちら（`@simplewebauthn/server`）で行い、通ったら Secret キーで
  マジックリンクを1本作って、その token_hash を自分で `verifyOtp` します
  （`lib/auth/passkey-session.ts`）。**メールは飛びません。**
- **鍵はドメインに紐づきます。** 本番で登録したパスキーは、プレビュー用の
  ドメインや `localhost` では使えません。仕様なので、こちらでは直せません。
- 許可アドレス（`ALLOWED_EMAILS`）の確認は、鍵が正しくても通します——
  パスワードの入口と同じ壁を立てています。

---

## 作ったけれど使っていない機能

- **アーカイブ検索** `/library` — タグ絞り込み込み。PGroonga で日本語が索引で引けます
- **自前の音声とスライド** `/listen` `/watch` — 4本あります（うち1本は上記のとおり失敗状態）
- **フィードの健康診断** — 設定画面。異常が無ければ何も出ません

---

## RSS の無いページ — 実測（8/26）と方針（8/27）

`回復術士のやり直し` のように、**サイトがRSSをやめてしまった**ページがある。
「更新があったことが分かればいい」という要望に対して、10ページで実測した。
（この節は owner-todo.md から移した。指示ではなく調べた結果なので、こちらが置き場所。）

- **本文テキストのハッシュを比べる方法は使える。** 10ページ中10ページでハッシュが
  安定し（3秒あけて2回取って変化なし）、8ページで「フィードの最新記事の題」が
  ページ本文に出ていた＝更新で必ず変わる
- **生HTMLのハッシュは使えない。** 4ページ中2ページが3秒で変わった（広告枠）
- **登録するURLの選定がすべて。** ComicWalker は素のURLだと「第1話」が出たままで
  **新話が出ても変わらない**。`?episodeType=latest` を付けると最新話が出る
- **どのURLが良いかを自動で判定する方法は見つからなかった。**
  「最近の日付が本文にあるか」で判定を書いたら11件中3件を間違え、しかも
  「絶対に鳴らないURL」を合格と判定した

**方針（8/27、オーナーの判断）**: 作る。**更新があったことだけを知らせる。**
中身は取りに行かない（RSS が無いページから本文を組み立てようとすると、広告や目次を
本文として掴んで、要約まで巻き添えになる）。出しかたは**普通の記事と同じ形**にして、
一覧に1行増やす——「RSS が無いので中身は取れていません」と記事の中身として書く。
専用の画面も通知の種類も増やさないので、スター・あとで・既読がそのまま使える。

**まだ実装していない。**まず ComicWalker の `?episodeType=latest` 1本で試す
（次の更新は9月上旬の見込み）。

---

## 見送った判断

無理に作らなかったものと、その理由。

- **pgvector で重複記事をまとめる** — 作る前に測ったら、直近400件で似た記事は4組（約1%）
  だけでした。1%のために毎記事へ埋め込みを付けるのは割に合いません
- **外部の本文抽出API（Jina Reader）** — 実際に困っている2フィード（Aeon・東洋経済）で
  両方とも弾かれました。効くのは元々取れているサイトだけです
- **エラーページ判定の定型文マーカー** — 誤検出が多く、8件中4〜5件が本物の記事でした。
  代わりに「冒頭が毎回同じ」で判定しています
- **PGroonga の `&@~` 演算子** — 索引を張るだけで8.2倍速くなったので、いまはそこまで
- **DB を触るテストの追加** — テストは外部依存の無い純関数だけに絞っています。
  `0027` の SQL は、本番のデータで実際に流して確かめました（そのほうが証拠として強い）

---

## フィードを入れ替えました（8/26）

**テスト用に私が足していた18本を全部外し、オーナーの Inoreader の OPML（16本）に
入れ替えました。**うち4本は死んでいたので解除し、**いまは12本**です。

| フィード | 記事 | 本文 | 平均字数 |
|---|---|---|---|
| 千葉県 新着情報 | 100 | 100 | 1,359 |
| 厚生労働省 新着情報 | 74 | 57 | 4,665 |
| 鎌ケ谷市ホームページ | 56 | 54 | 923 |
| dimeizaのブログ | 30 | 30 | 4,045 |
| プロジェクトマネジメントの話とかいろいろ（仮）※LIFE | 30 | 30 | 2,829 |
| ドカ食いダイスキ！ もちづきさん | 25 | 0 | — （漫画なので本文なし） |
| ゆうメンタルクリニック / HEADBOOST / Microsoft 365 Blog / 弁護士作花知志のブログ | 各10 | 各9〜10 | 930〜9,054 |
| 千葉県議会 新着情報 | 3 | 3 | 1,664 |
| ナゾロジー | 1 | — | 取り込み中 |

**本文は359件中313件（87%）で取れています。**漫画（ドカ食い）以外はほぼ全部です。
省庁・自治体の Shift_JIS も化けずに読めています（厚労省で平均4,665字）。

外した4本と理由:

| フィード | なぜ |
|---|---|
| 回復術士のやり直し - ヤングエースUP | フィードが404。サイトがRSSを廃止した（全パス404、宣言もなし） |
| Upcoming Microsoft Events | 404。サイトは移転し、フィードの提供なし |
| Microsoft Virtual Academy | ドメインごと消滅（サービス終了） |
| (タイトルなし) CiNii | フィードではなく論文1件の書誌データ（項目0件） |

**表示名は購読側に持たせました。**巡回はフィードの `<title>` で上書きするので、
放っておくと「千葉県 新着情報」が「トップページ」になります（実際になりました）。
購読側の表示名は巡回が触らないので、OPML の名前が残ります。

### ナゾロジーの過去記事90件は宙に浮いています

OPML の `nazology.net/feed` と、それまで入っていた `nazology.kusuguru.co.jp/feed` は
**別ホストで中身は同じ**です。`articles.url_hash` は全体で一意なので、過去記事は
古いフィード側に残ったまま新しい側には入りません。**オーナーの判断で、直さずそのまま**に
してあります（新着は次の巡回から普通に入ります）。

### 本文の取り直し（`0028`）

取れなかった記事は「処理済み」になり二度と取りに行かない作りでした。いまは理由を
残して、`network` と `short` のときだけ6時間後に1回だけ取り直します。
`recycled`（メニューを掴んだ）と `blocked`（403）は取り直しません。
経緯と実測は `docs/site-compat.md` の後半に。

### フィード別の「読めなさ」を設定画面に出しました

取得は 200 で成功し新着も毎日来るのに本文だけ取れないフィードは、**どの数字にも
出ません**でした（外す前の東洋経済は427件中347件＝81%がこの状態）。
いまは健康診断に「本文が取れない」として出ます（直近60日で半分を超えたフィードだけ、
10件以上あるもの）。

---

## 費用

すべて無料枠の中です。詳しくは `docs/quotas.md`。

- **要約（`gemini-3.5-flash-lite`）** — 1日 63〜93回で安定。失敗はほぼゼロ
- **台本（`gemini-3.5-flash`、1日20回）** — 8/17 を最後に使っていません。
  先に詰まるのはここなので、音声を作るときだけ気にすれば足ります
- **Storage** — 音声18ファイルで 5.2MB。無料枠1GBに対して 0.5%。14日で自動削除します

---

## 8/27 に直したこと — 日付と要約

### 千葉県の103件に日付が入っていなかった

フィードが `<pubDate>Wed, 26 Aug 2026 17:00:00 JST</pubDate>` と書いてきますが、
**`JST` は JavaScript が解釈できません**（RFC 822 が名前で認めているのは
UT / GMT と北米の4つだけ）。取り込みも本文も要約も正常に通り、日付だけが
欠けていました。一覧は日付なしを末尾に置くので、**103件（全体の29%）が
リストの一番下に沈んでいました**。「新着なのに出てこない」としか見えません。

- `lib/feeds/date.ts` で名前のタイムゾーンを読み替えます（JST / JDT / KST のみ。
  `CST` や `IST` は国によって違う時刻を指すので入れません）
- **既存の行も勝手に埋まります**（`0030`）。取り込みのついでに、その回の取得に
  入っていた項目ぶんだけ日付を入れ直します。手で直す作業は要りません
- **実際に埋まったことを確認済み**: 日付なし103件 → **0件**

### 先回りできないので、欠けたら見えるようにしました

タイムゾーンの略号を全部先に登録することはできません（`MSK` `CEST` …）。
健康診断が「日付が無い」フィードを出すようにしたので、次に知らない略号の
フィードを登録しても設定画面に出ます。

### 要約が「本文が取得できない」で埋まっていた

タイトルしか無い記事に要約を頼むと、モデルは*入力の説明*を返してきます。
一覧にそれが並び、無料枠も1件ぶん使います。

プロンプトで禁じるだけでは足りませんでした（24件→5件までしか減らない）。
**素材が空なら呼ばない**のが正解で、積む側と渡す側の両方で弾くようにしました。
購読中のフィードでは**0件**になっています。
