# あなたがやること

**このファイルは指示だけ。** いま何がどうなっているかは `docs/status.md` を見てください。

私（AI側）にはできないこと——ブラウザでの操作、外部サービスの設定画面、実機での確認、
好みの判断——だけを、急ぎの順に並べています。

最終更新: 2026-08-15（Supabase の設定完了を反映。穴は塞がりました）

---

## 0. 済んだこと（確認しました）

- ✅ **Supabase の URL 設定** — あなたが直してくださいました
- ✅ **新規サインアップの停止** — **こちらで実際に叩いて確認しました。**
  存在しないアドレスで `auth/v1/signup` を呼ぶと `422 signup_disabled` が返り、
  公開設定も `disable_signup: true` になっています。**穴は塞がっています**

残っているのは、あなたの好みや実機でしか分からないことだけです。

---

## 1. いますぐ（これをしないと入れません）

### 1-1. パスワードの初回設定

ログインをパスワードに変えたので、いまのアカウントにはまだパスワードがありません。
URL設定が直ったので、今度はメールが本番URLに戻ってきます。

1. https://rsstube.vercel.app/login を開く
2. メールアドレスを入れて「パスワードを設定・再設定する」を押す
3. 届いたメールのリンクを開く
4. パスワードを決める（8文字以上）

以後はスマホでもメールを見に行かずに入れます。変えたくなったら設定画面の「パスワード」から。

**サインアップは止まっていますが、この「再設定」は既存アカウント向けなので使えます。**
もし「そんなユーザーはいない」といった趣旨のエラーが出たら教えてください。

---

## 2. 実機・実耳でしか確かめられないこと

スマホから **https://rsstube.vercel.app** を開いてください
（LANの `192.168.1.5:3000` ではPWAも通知も動きません。HTTPSが要るため）。

- [ ] **通知をオンにして「テスト送信」** — いま登録端末は0件。これをやらないと朝の通知は
      どこにも飛びません。iPhone は共有メニュー →「ホーム画面に追加」で開いてからでないと登録できません
- [ ] **ホーム画面に追加して起動** — URLバー無しで開くか、スマホ幅のレイアウトが崩れていないか
- [ ] **スワイプ（左=既読 / 右=あとで）とスター** — **今回、押した手応えを出すように直しました。**
      押した瞬間に色が変わり、「スターを付けました」等の帯が下に出て、そこから取り消せます。
      指で確かめてほしいところです
- [ ] **記事に画像・動画が出るか** — 写真の多い記事（デイリーポータルZ・GIGAZINE）と、
      動画を貼っている記事で。**新しく取り込んだ記事から**効きます
- [ ] **`/listen` の46秒のサンプルを聴く** ← いちばん知りたい

### 音声について聞きたいこと

台本は**私が手で書いたもので、AI生成ではありません**（作った日に無料枠を使い切ったため）。
声と読み上げの質だけは、これで判断できます。

- 声の組み合わせは好みか（いま `Kore` と `Puck`。`GEMINI_TTS_VOICE_A` / `_B` で変えられます）
- 速さ・間合いは自然か
- **そもそも「2話者の対話」という方向でいいか**

---

## 3. 押すだけで済むもの

- [ ] **設定 → Google Drive に接続** — 認証情報は投入済みで、client_id / redirect_uri が
      有効なことは確認しました。同意はブラウザ操作なので私にはできません。
      押していただければ、実際にファイルが置けるところまで私が確認します
- [ ] **NotebookLM で音声概要を鳴らす** — `/exports` にダイジェストがあります。
      P2 の本命の受け入れテストです。自前音声と聴き比べて、**どちらを主にするか**を決めたい

---

## 4. 決めてほしいこと

- [ ] **フィードの整理** — テスト用に私が18本まで増やしました。ジャンルを広げるためで、
      あなたの好みで選んだものではありません。要らないものは購読をやめてください
      （設定画面から。スター等は残ります）
- [ ] **MDN Blog をどうするか** — 60日新着なし。取得は成功しているので壊れてはいません
- [ ] **設定画面が長すぎないか** — 項目が9つになりました。並べ替えか折りたたみが要るなら言ってください
- [ ] **読めないサイトを追いかけるか** — 110サイトを実測しました（`docs/site-compat.md`）。
      読めないのは9件で、すべて有料メディアか極端なボット対策です。**こちらで直せる余地は
      ありません。**購読したいものが含まれていたら教えてください。タイトルとリンクだけの
      扱いにするか、購読自体をやめるかを決めます

---

## 5. 時間が経てばできること

- [ ] **AI台本での音声生成をもう一度** — `gemini-3.5-flash` の枠は1日20回。
      太平洋時間の深夜（日本時間の夕方ごろ）に戻ります。戻ったら `/exports` の
      「アプリ内で音声にする」を押してください。確認したいのは**スライドが複数枚できるか**です
- [ ] **設定画面の使用量表を見る** — フィード18本を1時間ごとに巡回する定常状態です。
      1日あたりの消費量が溜まってきたら見てください

---

## 6. もし分かれば教えてほしいこと

- **消えたフォルダはどれか** — 前回お尋ねしたまま未解決です。こちらで見た範囲では
  `backups/`（6件）も `docs/`（6件）もあります。Google Drive の `RSSTube` フォルダは
  **まだ作られていません**（Drive 未接続のため。消えたのではなく、これからできます）。
  別の場所のことでしたら教えてください
- **`agy` の正しい使い方** — リポジトリのレビューに使おうとしましたが、実在しないファイル
  6件を指摘する出力でした（採用していません）。`--project` や `--add-dir`、モデル指定が
  要るのかもしれません。効くなら別視点のレビューとして価値があります
- **毎朝どの時間に聴くか** — いま生成時刻は6時。設定画面で変えられます
- **ダイジェスト8件は多いか少ないか** — 音声の長さに直結します
- **重要度の付き方に納得がいっているか** — 基準は要約プロンプトに書いてあり、「?」でも出ます。
  ずれていればプロンプトを直します
