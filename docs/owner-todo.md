# あなたがやること

**このファイルは指示だけ。** いま何がどうなっているかは `docs/status.md` を見てください。

私（AI側）にはできないこと——ブラウザでの操作、外部サービスの設定画面、実機での確認、
好みの判断——だけを、急ぎの順に並べています。

最終更新: 2026-08-14

---

## 1. いますぐ（スマホで使えない・穴が開いている）

### 1-1. Supabase の URL 設定を直す ← パスワード設定メールが localhost に飛ぶ原因

ログインはパスワードに変えましたが、**初回のパスワード設定メール**は同じ仕組みで
戻ってきます。アプリは正しく本番のURLを渡していますが、
**Supabase 側の許可リストに無いURLは Site URL に読み替えられます**。
ここを直さないと、スマホでパスワードを設定できません。

`https://supabase.com/dashboard/project/nrbykrzkwntbcbcwumew/auth/url-configuration`

1. **Site URL** を `https://rsstube.vercel.app` に変更
2. **Redirect URLs** に追加
   - `https://rsstube.vercel.app/**`
   - `http://localhost:3000/**` ← 消さないこと（手元の開発でログインできなくなる）

### 1-2. 新規サインアップを止める ← 誰でもアカウントを作れる状態

**パスワードに変えたぶん、ここの重みが増しました。**マジックリンクならリンクを踏ませる
手間がありましたが、パスワードなら `auth/v1/signup` を1回叩くだけでアカウントが作れます。

`disable_signup: false` を確認しました。**「Allow new users to sign up」がオンです。**

publishable key はクライアントに配る前提の公開鍵なので、誰でも取得できます。それで
`auth/v1/signup` を直接叩けば `ALLOWED_EMAILS` を素通りしてアカウントが作れます。
記事・要約・フィードは全ユーザー共通なので、入られると購読しているURLの一覧まで見えます。

Authentication → Sign In / Providers → Email →
**「Allow new users to sign up」をオフ**

（1-1 と同じ画面のすぐ近くなので、まとめてどうぞ）

**終わったら教えてください。** 実際に塞がったか、存在しないアドレスで `signup` を
叩いて確かめます。

---

## 2. 実機・実耳でしか確かめられないこと

スマホから **https://rsstube.vercel.app** を開いてください（LANの `192.168.1.5:3000`
ではPWAも通知も動きません。HTTPSが要るため）。1-1 を先に済ませないとログインできません。

- [ ] **通知をオンにして「テスト送信」** — いま登録端末は0件。これをやらないと朝の通知は
      どこにも飛びません。iPhone は共有メニュー →「ホーム画面に追加」で開いてからでないと登録できません
- [ ] **ホーム画面に追加して起動** — URLバー無しで開くか、スマホ幅のレイアウトが崩れていないか
- [ ] **一覧でスワイプ（左=既読 / 右=あとで）とスター** — 今日まで**動いていなかった**部分です。
      直したので効くはずですが、指で確かめてほしいところです
- [ ] **`/listen` の46秒のサンプルを聴く** ← いちばん知りたい

### 音声について聞きたいこと

台本は**私が手で書いたもので、AI生成ではありません**（作った日に無料枠を使い切ったため）。
声と読み上げの質だけは、これで判断できます。

- 声の組み合わせは好みか（いま `Kore` と `Puck`。`GEMINI_TTS_VOICE_A` / `_B` で変えられます）
- 速さ・間合いは自然か
- **そもそも「2話者の対話」という方向でいいか**

---

## 3. 押すだけで済むもの

- [ ] **設定 → Google Drive に接続** — 認証情報は投入済みで、client_id / redirect_uri が
      有効なことは確認しました。同意はブラウザ操作なので私にはできません。
      押していただければ、実際にファイルが置けるところまで私が確認します
- [ ] **NotebookLM で音声概要を鳴らす** — `/exports` に今日と昨日のダイジェストがあります。
      P2 の本命の受け入れテストです。自前音声と聴き比べて、**どちらを主にするか**を決めたい

---

## 4. 決めてほしいこと

- [ ] **フィードの整理** — テスト用に私が18本まで増やしました。ジャンルを広げるためで、
      あなたの好みで選んだものではありません。要らないものは購読をやめてください
      （設定画面から。スター等は残ります）
- [ ] **MDN Blog をどうするか** — 60日新着なし。取得は成功しているので壊れてはいません
- [ ] **設定画面が長すぎないか** — 項目が9つになりました。並べ替えか折りたたみが要るなら言ってください

---

## 5. 時間が経てばできること

- [ ] **AI台本での音声生成をもう一度** — `gemini-3.5-flash` の枠は1日20回で、今日は使い切り
      ました（10回中9回が失敗）。太平洋時間の深夜（日本時間の夕方ごろ）に戻ります。
      戻ったら `/exports` の「アプリ内で音声にする」を押してください。
      確認したいのは**スライドが複数枚できるか**です
- [ ] **明日、設定画面の使用量表を見る** — フィード18本を1時間ごとに巡回する定常状態が
      今日から始まりました。1日あたりの消費量はまだ誰も知りません

---

## 6. もし分かれば教えてほしいこと

- **`agy` の正しい使い方** — リポジトリのレビューに使おうとしましたが、実在しないファイル
  6件を指摘する出力でした（採用していません）。`--project` や `--add-dir`、モデル指定が
  要るのかもしれません。効くなら別視点のレビューとして価値があります
- **毎朝どの時間に聴くか** — いま生成時刻は6時。設定画面で変えられます
- **ダイジェスト8件は多いか少ないか** — 音声の長さに直結します
- **重要度の付き方に納得がいっているか** — 基準は要約プロンプトに書いてあり、「?」でも出ます。
  ずれていればプロンプトを直します

---

## 追記: パスワードの初回設定（1-1 のあと）

ログインをパスワードに変えました。いまのアカウントにはまだパスワードが無いので、
**1-1 を直したあとに1回だけ設定**してください。

1. https://rsstube.vercel.app/login を開く
2. メールアドレスを入れて「パスワードを設定・再設定する」を押す
3. 届いたメールのリンクを開く（本番URLに戻ってくるはず。戻らなければ 1-1 が未完了）
4. パスワードを決める（8文字以上）

以後はスマホでもメールを見に行かずに入れます。変えたくなったら
設定画面の「パスワード」から。
