# あなたがやること

**このファイルは指示だけ。** いま何がどうなっているかは `docs/status.md` を見てください。

私（AI側）にはできないこと——ブラウザでの操作、外部サービスの設定画面、実機での確認、
好みの判断——だけを、急ぎの順に並べています。**済んだものは消します**（履歴は
`docs/status.md` と git log に残ります）。

最終更新: 2026-09-08（サーバー側は 246ms。まだ3秒とのことなので、実機に測らせる仕組みを入れました）

---

## 1. Google Drive を繋ぐ（本番の設定画面で、5分）

繋がらなかったのは同意がまだだからではなく、**本番に認証情報が入っていなかった**から。
ご指摘のとおり Vercel の環境変数は触れる人が限られるので、**設定画面から入れられるように
変えました**（`0033`）。Vercel を触る必要はもうありません。

- [ ] **[本番の設定画面](https://rsstube.vercel.app/settings?drive=unconfigured) →
      Google Drive →「Google の認証情報」**に、`.env.local` と同じ
      **クライアント ID とシークレット**を入れて保存（手元の値をそのまま使えます。
      Google Cloud Console から取り直す必要はありません）
- [ ] **[Google Cloud Console](https://console.cloud.google.com/apis/library) → 認証情報 →
      OAuth クライアント →「承認済みのリダイレクト URI」**に
      `https://rsstube.vercel.app/api/auth/google/callback` を追加（localhost のぶんは残したまま。
      **登録すべき文字列は設定画面にそのまま出しています**）
      - 認証情報の画面には
        <https://console.cloud.google.com/apis/credentials> で直接飛べます
- [ ] **「Google Drive に接続」を押す**

押していただければ、実際にファイルが置けるところまで私が確認します。
**OAuth クライアントはアプリに1つで足ります**——2人目からは接続ボタンを押すだけです。

---

## 2. 実機でしか確かめられないこと

スマホから **https://rsstube.vercel.app** を開いてください
（LANの `192.168.1.5:3000` ではPWAも通知も動きません。HTTPSが要るため）。

- [ ] **起動の内訳を測って、数字を送ってください（9/8）** — サーバー側は 3029ms → 246ms に
      なったのに、まだ3秒とのことでした。**ここから先は実機でしか出ません**ので、
      端末に測らせる仕組みを入れました。手順は3つです:
      1. iPhone のホーム画面のアイコンから開く（**アプリを一度終了してから**。
         上にスワイプして消す。裏で生きたまま開くと、冷えた起動になりません）
      2. 一覧が出たら、**数秒待ってから**「設定」→ **いちばん下の「起動の内訳」**
      3. 「数字をコピー」を押して、その中身を送ってください
      **1回で足りません**——3〜4回、間を空けて（できれば朝も1回）測ってください。
      記録は端末の中だけに残り、サーバーへは送っていません。
      これで「サービスワーカーの起動」「通信」「描画」「操作できるまで」のどこに
      時間が乗っているかが分かります。**いまはどれが犯人か分かっていません**ので、
      推測で直すより先にここを見たいです
- [ ] **通知をオンにして「テスト送信」** — 登録端末は**0件のまま**です。
      これをやらないと朝の通知はどこにも飛びません。iPhone は共有メニュー →
      「ホーム画面に追加」で開いてからでないと登録できません
- [ ] **ホーム画面に追加して起動** — URLバー無しで開くか、スマホ幅のレイアウトが崩れていないか
- [ ] **一覧を下までスクロールする** — 8/27 に無限スクロールを入れました。先頭60件で
      止まっていたのが、下端が近づくと60件ずつ増えます。**スマホで効くかを見たいところです**
- [ ] **一覧に ★ ▤ の印が出るか（9/7 に直しました）** — スターを付けた記事に ★、
      書き出した記事に ▤ が一覧の行に出ます。**これまで出ていませんでした**
      （既読の行も未読と同じ見た目でした）。記事の状態を DB から受け取る形が
      こちらの想定と違っていたためで、印が出ないだけで動作は壊れていなかったので
      気づきにくいものでした。**印が付くかを見てください**
- [ ] **右スワイプがスターになりました（9/7）** — 「あとで読む」をやめたので、
      一覧の行を右へ払うとスターが付きます（左へ払うと既読は今までどおり）。
      指が「あとで」を覚えていると戸惑うかもしれません。**別の動作にしたい、
      右スワイプ自体を無くしたい、があれば言ってください**
- [ ] **スワイプ（左=既読 / 右=あとで）とスター** — 押した瞬間に色が変わり、
      帯が下に出て、そこから取り消せます。指で確かめてほしいところです
- [ ] **パスキーを登録して、ログインし直す** — 設定画面の「パスキー」で
      「この端末を登録」を押し、名前（例: iPhone）を付けてください。
      そのあと一度ログアウトして、ログイン画面の「パスキーでログイン」で入れるか。
      **パスワードは残っています**ので、うまくいかなくてもこれまでどおり入れます
- [ ] **本文を横に払って次の記事へ行けるか** — 本文の上で左へ払うと次、右へ払うと前。
      **前へ戻るときは画面の左端からではなく真ん中あたりから**払ってください
      （端は Safari の「戻る」が優先されて奪えません）
- [ ] **上の帯の記号が分かるか** — スマホでは記号だけ（`←` `★` `◷` `▤` `♪` `↗`）に
      しました。手元の375px幅で41pxです。**どれが何か分からないものがあれば教えてください**
      （そこだけ文字に戻せます）
- [ ] **下の「前の記事 / 次の記事」** — 高さを **56px** にしました（9/7。41px → 48px → 56px）。
      真ん中に記事のメニュー（☰）も入っています。まだ押しにくければ言ってください
- [ ] **文字の大きさがちょうどか** — 全体を2段階上げました。大きすぎる／
      まだ小さいがあれば、そう言ってください（目盛りを1か所直せば全部変わります）
- [ ] **長い記事を途中で閉じて、開き直す（9/7 に入れました）** — 議事録のような記事を
      途中まで読んで一覧へ戻り、もう一度開くと**前回の位置から**出ます。
      「前回の続きから開きました／先頭へ」が5秒だけ出ます。戻りすぎ・戻らなすぎがあれば
- [ ] **機内モードで開いてみる（9/7 に入れました）** — アプリを1分ほど開いたあと、
      機内モードにして開き直すと「圏外です」の下に未読20件が並び、本文まで読めます。
      **通勤で使えるかを見たいところです。** 画像は入っていません（圏外では取れないため）。
      既読も付きません（電波のあるところで開き直すと本体に戻ります）
- [ ] **記事に画像・動画が出るか** — 写真の多い記事（デイリーポータルZ・GIGAZINE）と、
      動画を貼っている記事で

---

## 3. 押すだけ・聴くだけ

**毎朝のまとめは NotebookLM で聴く**と決まりました（8/31）。なので毎朝の自前音声は
自動生成しません。自前音声は「押したときだけ作る深掘り用」として残ります。

- [ ] **NotebookLM で1回鳴らしてみる** — `/exports` に朝のぶんが溜まっています。行を押すと
      「.md をダウンロード / 本文をコピー / 指示文をコピー / NotebookLM を開く」が1画面に出ます。
      **これが P2 の受け入れテストです。** 渡す手数が多すぎたら、そこを削ります
- [ ] **手数を減らすなら先に Drive**（上の「1」）— 繋いであれば、朝のダイジェストを
      毎朝そのまま Drive に置けます。NotebookLM 側は「ソースを追加 → ドライブ → 選ぶ」だけになり、
      落として上げ直す2手が消えます。**やりますか**（Drive が繋がってからの作業です）
- [ ] **声を聴いて選ぶ** — 深掘り用に残るので、気が向いたときで構いません。設定画面から
      6種類（`0032`）。聴き比べは `samples/voices/`。いまは既定の Kore / Puck です

---

## 4. 決めてほしいこと

- [ ] **フィードのフォルダ分け** — 12本のうち**11本が「未分類」**です（8/26 の入れ替えで
      割り当てが引き継がれませんでした）。フォルダ名だけが10個空で残っていたので、
      サイドバーからは隠しました（`#77`）。振り分けるか、使わないフォルダを消すか。
      **どのフィードをどのカテゴリに入れるかは好みなので、方針をいただければ当てます**
- [ ] **監視したいページのURL** — RSS の無いページの監視をこれから作ります（下の「6」）。
      まず ComicWalker の `?episodeType=latest` で試しますが、他にもあれば教えてください。
      **URLの選び方がすべてです**（素のURLだと「第1話」が出たままで、新話が出ても
      ページが変わらないことがあります）
- [ ] **設定画面が長すぎないか** — 項目が11個になりました。並べ替えか折りたたみが要るなら言ってください
- [ ] **ダイジェスト8件は多いか少ないか** — 音声の長さに直結します
- [ ] **毎朝どの時間に聴くか** — いま生成時刻は6時。設定画面で変えられます
- [ ] **ダイジェストが新着順になりました** — 重要度を廃止した（`0037`）ので、
      選抜は「過去24時間の未読から、1つのフィードに偏らないように新しい順」です。
      偏り防止の上限は**フィードごと**にしてあるので（2026-09-03）、フォルダ分けは
      しなくても効きます。これで朝のまとめが薄くなるようなら、選び方をもう一度
      考えます

---

## 5. 前にお尋ねしたまま未解決

- [ ] **消えたフォルダはどれか** — こちらで見た範囲では `backups/` も `docs/` もあります。
      Google Drive の `RSSTube` フォルダは**まだ作られていません**（Drive 未接続のため。
      消えたのではなく、これからできます）。別の場所のことでしたら教えてください

---

## 6. これから私が作るもの（作業は不要。方針の確認だけ）

**RSS の無いページの監視。** 決まったことは「**更新があったことだけを知らせる**」。
中身は取りに行きません（RSS が無いページから本文を組み立てようとすると、広告や目次を
本文として掴んで、要約まで巻き添えになる）。出しかたは**普通の記事と同じ形**にして、
一覧に1行増やします——「RSS が無いので中身は取れていません。更新があったことだけ
お知らせします」と記事の中身として書く形です。スター・あとで・既読がそのまま使えます。

実測の詳細（本文ハッシュは安定・生HTMLは3秒で変わる・URL選定がすべて）は
`docs/status.md` の見送り判断のところに残してあります。

---

## 7. 急がないが、いつか要る作業

**Google Cloud TTS への移行。** Gemini TTS は preview なので、いつ無料枠が変わっても
おかしくありません。移り先は Google Cloud TTS（無料枠400万文字/月）ですが、
**APIキーでは叩けません**（`GEMINI_API_KEY` で試すと 401 で
「API keys are not supported by this API」）。GCPプロジェクトで [Cloud Text-to-Speech API](https://console.cloud.google.com/apis/library) を
有効にし、サービスアカウントの鍵を発行して入れる必要があります。**いま急ぐ必要はありません。**

---

## 8. 既にある記事にも「本体はPDF」を効かせる（実行の判断だけ）

厚労省のように **HTMLには表題とリンクしか無く、中身はPDF（や別ページ）にある記事**を
読めるようにしました。これから入る記事には自動で効きますが、**既に取り込んだぶんは
取り直しません**（`extracted_at` が入っている記事は二度と取りに行かない作りのため）。
いま50件が、フッターや「言語切替」の案内を本文として持ったまま残っています
（うち20件が厚労省）。

```
node --env-file=.env.local scripts/reextract-thin.mjs               # 対象を数えるだけ
node --env-file=.env.local scripts/reextract-thin.mjs --run --limit 10   # 10件だけ試す
```

- [ ] まず `--limit 10` で流し、出来上がった要約を見てから残りを流すか決める
      （要約は Gemini の無料枠を使うので、一度に全部は積まないこと）
