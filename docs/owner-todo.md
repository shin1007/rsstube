# あなたにお願いしたいこと

私（AI側）ではできない、または勝手に決めるべきでないものだけを集めた。
コードを書く作業はここには入れていない（それは私がやる）。

最終更新: 2026-08-14

---

## いまの状態（これは済んでいる）

同じことを二度やらないように、先に済んでいることを書いておく。

- 本番は自動で回っている。pg_cron が5本（巡回・ワーカー・ダイジェスト・掃除2種）動いていて、
  実際に発火して成功するところまで確認済み
- Vercel の環境変数（VAPID 3つ含む）は投入済み。`CRON_SECRET` は手元と同じ値に揃えてある
- マイグレーションは `0013` まで本番に適用済み（**本番と手元は同じ Supabase**）
- `settings` の行も作成済み（生成時刻6時・8件）
- フィード18本・記事843件・ダイジェスト2本
- 日本語の全文検索を PGroonga にした（#28）。`/library` の本文検索が8.2倍速
- 「まだ処理していない」と「抽出に失敗した」を区別できるようにした（#27）。
  設定画面に「取り込みの進み具合」が出る

---

## 1. 実機・実耳でしか確かめられないこと

コードは書いてあるが、**動くかどうかを私が確認できない**もの。ここが一番の空白。

### 1-1. 通知が実際に届くか

いま `push_subscriptions` は **0件**。端末を登録しないと、朝のダイジェストの通知は
誰にも飛ばない（送信処理は動いていて、送り先が無いだけ）。

1. 設定画面 →「ダイジェストの通知」→「通知をオンにする」
2. 「テスト送信」を押す
3. 届かなければ、画面に出ている状態（未対応／ホーム画面に追加が必要／ブロック済み）を教えてほしい

**iPhone / iPad は、Safari のタブで開いている間は登録できない。**
共有メニューから「ホーム画面に追加」して、そこから開く必要がある。

### 1-2. スマホでの見え方と PWA

スマホ幅のレイアウトは実装してあるが、一度も実機で見ていない。
ホーム画面に追加して、URLバー無しで起動するかも未確認。

見てほしいところ: 記事リストのスワイプ（左で既読・右であとで）、下部タブ6つが窮屈でないか、
`/watch` の再生画面が縦画面で成立しているか。

### 1-3. 生成した音声を聴く ← これが一番知りたい

`/listen` に「【動作確認用】手書き台本のサンプル音声」（46秒）がある。
**台本は私が手で書いたもので、AI生成ではない**（作った当日に無料枠を使い切ったため）。
声と読み上げの質だけは、これで判断できる。

聴いたうえで教えてほしいこと:

- 声の組み合わせは好みか（いまは `Kore` と `Puck`。`GEMINI_TTS_VOICE_A` / `_B` で差し替えられる）
- 速さ・間合いは自然か
- そもそも**この方向（2話者の対話）でいいか**

### 1-4. NotebookLM と聴き比べる

`/exports` に今日と昨日のダイジェストがある。`.md` を落として NotebookLM に入れ、
音声概要を鳴らしてみてほしい。**P2 の本命の受け入れテストがこれ。**

聴き比べたうえで「どちらを主にするか」を決めたい。出来上がりは NotebookLM のほうが
上のはずで、その場合は自前音声（P4/P5）の位置づけが「手で開くのが面倒な日の代替」になる。

---

## 2. 決めてほしいこと

私が勝手に決めるべきでないもの。

### 2-1. 次に何を作るか

**上位2つ（PGroonga・未処理と失敗の区別）は実装して main に入れた**（#27・#28）。
残りは下のとおりで、どれも「やるべき理由が弱い」か「あなたの手が要る」もの。

| 候補 | 状態 |
|---|---|
| **PGroonga** で日本語検索 | **完了**（#28）。索引を張るだけで本文検索が 80.8ms → 9.9ms |
| **未処理と失敗の区別** | **完了**（#27）。設定画面に「取り込みの進み具合」も追加 |
| **pgvector** で重複まとめ | **見送りを提案**。下記のとおり重複が少なすぎる |
| **Google Drive 連携** | あなたの手が要る（下の 2-3） |
| Hacker News API | 重要度スコアの補正。英語圏に偏るので日本語フィードには効かない |
| 外部の本文抽出API | **候補から外した**。Jina Reader を試したが、実際に困っている
  2フィード（Aeon・東洋経済）で両方とも弾かれた |

**pgvector を見送る理由**: 作る前に重複の量を測ったところ、直近400件・フィード横断で
**似た記事は4組（約1%）**しかなかった。しかも単純な語の重なりで見つかる程度で、
埋め込みを持ち出すまでもない。

見つかった4組はこういうもの（本物の重複ではある）:

```
GIGAZINE「中華AIスタートアップのDeepSeekが…」 ／ Hacker News「DeepSeek V4 Pro 0813」
GIGAZINE「イルカが貝殻を使って魚を捕らえる…」 ／ ナゾロジー「イルカが貝殻を使って…」
```

**フィードをもっと増やして重なりが目立ってきたら、そのとき作るのが正しい。**
いま作っても、1%のために毎記事へ埋め込みを付けることになる。

### 2-2. フィードの整理

テスト用に私が18本まで増やした。ジャンルを広げるためで、あなたの好みで選んだものではない。
**要らないものは購読をやめてほしい**（設定画面から。スター等は残る）。

いま気になっているのは1本だけ:

- **MDN Blog** — 60日新着なし。取得は成功しているので壊れてはいない。残すかどうか

### 2-3. Google Drive をやるかどうか

やるなら Google Cloud Console での作業が要る（私にはできない）:

1. プロジェクトを作る（既存でも可）
2. OAuth クライアント ID を作る（種別: ウェブアプリケーション）
3. スコープは `drive.file`（自アプリが作ったファイルだけ。全ドライブは要らない）
4. リダイレクトURIに `https://rsstube.vercel.app/api/auth/google/callback` と
   `http://localhost:3000/api/auth/google/callback` を登録
5. クライアントIDとシークレットを教えてもらえれば、あとは私が組む

plan.md には最初から「面倒なら後回しでよい」と書いてある項目なので、
気が向かなければ飛ばして構わない。

---

## 3. 積み残しの検証（時間が経てばできる）

### 3-1. AI台本での音声生成をもう一度

`gemini-3.5-flash` の無料枠は**1日20リクエスト**で、スキーマの調査で使い切った日があった
（今日の使用量: 10回中9回が失敗＝枠切れ）。枠は太平洋時間の深夜（日本時間の夕方ごろ）に戻る。

戻ったら `/exports` の「アプリ内で音声にする」を押してほしい。確認したいのは
**スライドが複数枚できるか**。一度成功した回は1枚しか返らず、そこから
「1スライドが複数セグメントに割れる」形に直したが、その修正後の生成をまだ回せていない。

### 3-2. 要約の追いつき

私がフィードを増やしたぶんの順番待ちが残っている（設定画面の「取り込みの進み具合」で
今の数が見える）。ワーカーが5分ごとに消化するので、放っておけば減る。

**明日また使用量の表を見て**、1日あたりの呼び出し回数が無料枠に対してどうかを
確かめてほしい。フィード18本を1時間ごとに巡回する状態は今日が初めてなので、
定常状態の消費量はまだ誰も知らない。

---

## 4. たまに見てほしいもの

- **設定画面の使用量表** — 失敗が並んでいたら無料枠に当たっている合図
  （429 でジョブが待たされ、要約や音声が静かに遅れる）
- **設定画面の「気になるフィード」** — 何も無ければ何も出ない。出ていたら手当ての合図
- `select * from cron.job_run_details order by start_time desc limit 20;` — pg_cron が
  失敗していないか

詳しくは `docs/quotas.md`（無料枠と詰まりの見分けかた）。

---

## 5. 私が把握していない前提があれば教えてほしい

- 毎朝どの時間に聴くか（いま生成時刻は6時。前倒し・後ろ倒しは設定画面で変えられる）
- ダイジェストの件数8件は多いか少ないか（音声の長さに直結する）
- 「重要度」の付き方に納得がいっているか（基準は要約プロンプトに書いてあり、
  `?` を押すと画面にも出る。ずれていればプロンプトを直す）
