@AGENTS.md

# RSSTube — プロジェクトの前提

AI要約つきの個人用RSSリーダー。

このファイルには**決まったこと**と、**踏んだ罠の索引**だけを書く。
以下は別に分けてある:

| 知りたいこと | ファイル |
|---|---|
| 同じ壊れ方をもう一度しないために（44件） | `docs/traps/`（索引は下） |
| いま何がどうなっているか（報告） | `docs/status.md` |
| オーナーがやること（指示） | `docs/owner-todo.md` |
| 無料枠と、詰まったときの見分けかた | `docs/quotas.md` |
| 音声・スライドの他サービス比較 | `docs/tts-options.md` |
| 競合と比べて足りないもの（改善提案） | `docs/usability.md` |
| どのサイトの本文が取れるか（110件実測） | `docs/site-compat.md` |
| 設計の全体像 | `docs/plan.md` |
| セットアップと使い方 | `README.md` |

**状態を2か所に書かない。**片方が必ず古くなるので、報告は `status.md` に、
指示は `owner-todo.md` に集めること。

## 決まっていること（蒸し返さない）

- **NotebookLM に公開APIは無い**（2026年8月時点。Enterprise版のみ、コンシューマ版は未提供）。
  なので「アプリで整形 → 人が NotebookLM に投入」が P2 の方針。自前音声は後段（P4、実装済み）。
  2026-07 に「Gemini Notebook」へ改称し、Video Overview / Slide Deck / Infographic なども
  出せるようになったが、**API が無いことは変わっていない**ので自動化の対象にはならない。
  出来上がりは向こうが上なので、書き出しの経路は残し続ける。
  音声・スライドの他サービスの比較は `docs/tts-options.md`。結論は「いまは変えない、
  Gemini TTS の preview が終わったら Google Cloud TTS」。
  **毎朝のまとめは NotebookLM で聴く**（2026-08-31 決定）。選抜と Markdown までを
  こちらが自動で作り、音声にするのは人が NotebookLM でやる。**毎朝の自前音声を
  自動生成する経路は作らない**——出来上がりは向こうが上で、聴かれないものに
  `gemini-3.5-flash` の1日20回と Storage を毎日使うことになる。自前音声は残すが、
  位置づけは「押したときだけ作る深掘り用」（記事1本・手で選んだまとめ）に下がる。
- **ダイジェストの音声は「15分のまとめ番組」**（2026-09-01 決定）。記事を1本ずつ
  順に紹介する形にしない——冒頭で全体像、続いて**話題ごと**（関連する記事は同じ話題に
  まとめる）、最後に締め。尺は件数×2.5分を6〜15分に収める（`lib/ai/script.ts` の `plan()`）。
  **話題の数は記事数の2/3にすること**——同数にすると枠がちょうど足りてしまい、
  必ず1記事1話題（＝元の読み上げの列）に戻る。
- **AI は Gemini API の無料枠**を使う。要約 `gemini-3.5-flash-lite`、台本 `gemini-3.5-flash`、
  音声（後段）`gemini-3.1-flash-tts-preview`。いずれも無料枠あり。
- **定期実行は Supabase の `pg_cron`**。Vercel Hobby の cron は1日1回までで
  1時間毎の巡回ができないため。`supabase/scheduler.sql` を参照。
- **Next.js 16.3**。`middleware` は `proxy.ts` に改称、`params`/`searchParams` は Promise、
  Turbopack が既定。訓練データと差異があるので `node_modules/next/dist/docs/` を読むこと（AGENTS.md）。
- **ダークテーマ固定**。朝晩に長時間読む用途のため、切り替えは作らない。
- **記事・要約・フィードは全ユーザー共通**（`0005`/`0006` で実施済み）。
  ユーザーごとなのは `subscriptions` / `article_states` / `folders` /
  `exports` / `digests` / `settings` だけ。ユーザーごとに複製していた頃は
  記事178件＝3.7MB（本文平均8KB）、要約は `BATCH_SIZE = 5` で約36回の Gemini 呼び出しが
  購読者数ぶん掛かる形で、無料枠に収まらなかった。2人目が同じフィードを購読しても
  記事・ジョブは増えず `article_states` だけが増えることを実測で確認済み。
  - `feeds` に書き込みポリシーは無い。購読の入口は `subscribe_feed()` /
    `unsubscribe_feed()` RPC だけ。タイトルや etag の持ち主は巡回であって購読者ではない。
  - 副作用として、ログイン済みなら**全フィードのURLが見える**（誰が購読しているかは見えない）。
    購読 URL 自体を隠したくなったら、そのとき `feeds` の select を絞ること。
  - 要約は共通なので**言語も全体で1つ**しか選べない。当面はオーナーの
    `settings.summary_language` を使う。ユーザーごとに変えるなら
    `summaries` を `(article_id, language)` で持つ必要がある。
- **毎朝ダイジェストの生成時刻はアプリ側で判定する**。pg_cron から毎時叩き、
  各ユーザーの `settings.digest_hour`（日本時間）を**過ぎていて**当日ぶんがまだ
  無ければ作る。「ちょうどその時刻か」で見ないのは、pg_net の失敗やデプロイ中の
  1回で cron が落ちただけでその日のダイジェストが永久に作られなくなるため
  （次の実行はもう別の時刻になる）。過ぎていれば作る形なら自分で追いつく。
  cron の式は1つしか置けないので、ユーザーごとの時刻を式では表せない。
  Vercel Cron を使わないのは1日1回制限に加えて実行時刻が±59分ずれるから
  （6時のダイジェストが7時前にできてしまう）。
- **重要度は作らない**（2026-09-03 決定、`0037`。**蒸し返さない**）。
  AI に 0〜100 を付けさせ、一覧の並べ替えとダイジェストの選抜に使っていたが、
  列ごと落とした。**重要度は記事の属性ではない**——同じ記事でも職業が違えば重さは
  違うし、同じ部門でも立場が違えば変わる。読み手との関係で決まるものを記事の側に
  1つの数値として持たせたのが、そもそもの型の誤りだった。
  - 途中で試した**フォルダごとの重み**（`0036`、1日で撤去）も外れ。実データで
    話題の軸と情報源の軸は直交していた——「AI」タグの記事は購読12本のうち12本に、
    「健康」は13本に散っている。フィードの束に重みを掛けても関心は表現できない。
    **タグに重みを付ける案も同じ穴**で、記事に付いた一つの尺度を人が後から曲げる形に
    変わりはない。
  - **数値を残したまま使わない、はしないこと。** 画面に出ていれば読む人は必ず
    意味を探す。やめるなら列ごと落とす。
  - 要約プロンプトにも「価値を評価しない・点数を付けない」と書いてある。
    再導入したくなったら、まず「誰にとっての重要度か」に答えられるか確かめること。
  - 代わりに: 一覧の並びは**新着順だけ**。ダイジェストの選抜は**新しい順**で、
    フォルダごとの上限（全体の1/3）だけ残してある。偏り対策は重要度と関係なく要る。
- **ダイジェストを作り直すのは「digests の当日行を消してから」**。
  書き出した記事には `exported_at` が付いて選抜から外れるので、`force=1` を
  もう一度叩いても同じものは再現せず、中身の違う2本目ができて1本目が宙に浮く。
  なので `force=1` は生成時刻だけを無視し、当日ぶんの有無は必ず見る。
- **自前音声はセグメント（クリップ）単位で作って保存する**。台本をスライド1枚ぶんに
  区切り、長ければさらに700字ごとに割る。切れ目がそのままスライドの切り替わりに
  なるので、再生側は時刻からスライドを逆算しなくてよい。TTS の1回も短く済み、
  失敗時の再試行が安い。`media_segments` が `idx` と `slide_idx` を別に持つのは、
  1スライドが複数セグメントに割れるため（モデルがスライドを1枚しか返さないことがある）。
- **mp4 は作らない**。スライドは HTML/CSS で描く（plan.md §4）。サーバー側の合成が
  要らず、スマホでも即出る。将来 mp4 が要るなら、同じスライドJSONと
  セグメント音声がそのまま素材になる。
- **ログインの入口は2つ、パスワードとパスキー**（`0034`）。**パスワードを消さない。**
  パスキーは端末に紐づくので、端末を替えたときと生体認証が通らないときに戻る先が要る。
  **Supabase の Auth に WebAuthn は無い**（2026-08 時点）ので、署名の検証は
  `@simplewebauthn/server` でこちらが行い、通ったら Secret キーで
  `generateLink({type:'magiclink'})` を1本作って、その `hashed_token` を自分で
  `verifyOtp` してセッションにする（`lib/auth/passkey-session.ts`。メールは飛ばない）。
  **Cookie を書くので Route Handler から呼ぶこと**——Server Component からだと
  セッションが付かないまま成功が返る。RP ID と origin は環境変数に書かず、
  いま開いている URL から組み立てる（Google の redirect_uri と同じ理由）。
  副作用として**鍵はドメインに紐づく**ので、本番で登録したパスキーは
  プレビューや localhost では使えない。仕様なので直せない。
- **当面は個人用のまま**（2026-08-13 決定）。一般公開は「自分が毎日使えている」状態に
  なってから。費用は無料枠に収まる範囲で設計する。

## 出し方（2026-08-31 決定）

**直したら、確認を待たずに本番まで出す。** レビューするのは本人だけなので、
PR を出したまま止めると、マージのためだけにもう一往復頼むことになる。
そして**本番に出るまで、オーナーはスマホで確かめられない**——手元で直っていても、
デプロイされていなければ「直っていない」のと同じに見える。

1. 検査を**先に**通す（`npm run typecheck` / `npx eslint src` / `npx vitest run` / `npm run build`）。
   auto-merge は CI が通った瞬間に入るので、出したあとに直す隙は無い
2. ブランチを切ってコミット → `git push` → `gh pr create`
3. `gh pr merge <番号> --auto --squash` まで**続けて**実行する
4. マージされたら `gh run list` で main の CI を見て、Vercel のデプロイ（2〜3分）を待つ
5. **本番で1回動かす。** ログイン不要の確認口は
   `CHECK_URL_BASE=https://rsstube.vercel.app node --env-file=.env.local scripts/check-url.mjs <URL>`
   （`CRON_SECRET` で守られた `/api/debug/extract` を通る）。
   **手元で通ることと Vercel で動くことは別物**——`jsdom` が本番だけ
   `ERR_REQUIRE_ESM` で落ちた前科がある。依存を足した回は必ずここまでやる
6. 実機でしか見られないもの（PWA・通知・指の操作）は、**どの画面のどこを見るか**を
   書いて渡す。「確認してください」だけでは伝わらない

画面の確認は、パスワードを預からなくてもできる。Secret キーで
`generateLink` → `verifyOtp` してセッションを作り、`sb-<ref>-auth-token` の Cookie に
して browser-drive の `storageState` に渡す（`lib/auth/passkey-session.ts` と同じ手口）。
**使い終わったら state ファイルを消すこと**——中身は生きたトークン。

## 環境

- **このプロジェクトは `C:\dev\rsstube` で開発する。`G:\マイドライブ` 配下には置かない。**
  Google Drive のストリーミングドライブでは `npm install` が `EPERM`→`EBADF` で必ず失敗し、
  `node_modules` を逃がすジャンクションも作れない（Drive の仮想FSが再解析ポイント非対応）。
  同じ install が Drive で10分超＋失敗、`C:\dev` で22秒。
- `G:\マイドライブ\ローカルリポジトリ\RSSTube` に壊れかけの旧コピーが残っている。削除してよい。
- `.env.local` は実値が入っている。Supabase・Gemini とも接続済み。

## いまの状態

**状態と検証状況は `docs/status.md`、やってほしいことは `docs/owner-todo.md`。**
ここに写すと二重管理になって片方が古くなるので、この2つを見ること。

ごく手短にいえば、コードは P0〜P5 まで揃っていて（plan.md に未実装は無い）、
本番は pg_cron で自動運転している。残っているのは実機での確認と、外部サービスの設定。

## 踏んだ罠 — 索引

中身は `docs/traps/` に分けてある。**この索引の見出しに当たる場所を触るときは、
必ず該当ファイルを開くこと。** 見出しは「何が起きたか」しか書いていない——
どう直すか、なぜそうするか、どこで実測したかは向こうにある。索引だけ読んで
手を動かすと同じ罠をもう一度踏む（見出しを見ただけで避けられるなら、
そもそも罠になっていない）。

新しく踏んだら、分野のファイルに足してここに1行を増やすこと。
どの分野にも入らないものが出てきたら、そのときファイルを1つ増やす。

### 取り込みと本文抽出 — `docs/traps/feeds.md`

- `res.text()` は日本語のサイトで文字化けする。
- 本文が取れないサイトに、こちら側の直しどころは無い……が、3つに分けて数えること。
- フィード単位の「読めなさ」はどの数字にも出ない。
- 巡回は `feeds` を直に見てはいけない（`0029`）。
- `articles.url_hash` は全体で一意。
- `JST` は JavaScript が解釈できない。
- 要約に「本文が取れなかった」ことを書かせない。
- 直したら、それが既存の行にも届くか確かめること。
- 先回りで塞げないものは、欠けたことが見えるようにする。
- 消毒で枠を外した子は、もう一度見直すこと。
- HTMLに本文が無いページは、本体が別にある（PDF・別ページ）。
- 本文が短い記事では、Readability がメニューのほうを本文として掴む。
- 枠を掴んだ記事は、既に入っているぶんが直らない。

### Gemini と無料枠 — `docs/traps/ai.md`

- `responseSchema` の `maxItems` は「項目3つまで」なら使える。
- `maxOutputTokens` は思考トークンも含む。
- 長さは「合計で何字」ではなく「1枠に何字」で頼む。
- `gemini-3.5-flash` の無料枠は1日20リクエスト。

### ジョブ・ワーカー・音声 — `docs/traps/jobs.md`

- 1回のワーカー実行は時間で切る。
- VAPID 鍵を作り直すと登録済みの端末には二度と届かない。
- `jobs` にはRLSポリシーが1つも無い。
- スライドは文字だけ、と気づかれない。
- 後から全記事を浚う処理を足さない。
- 引いたジョブを全部処理しないなら、その場で戻すこと。
- 待ち時間の見当は件数の上限からではなく実測から出す。
- ジョブが諦めたら、それを待っている行も誰かが落とすこと。
- 作り直しは、できている途中まで残して再開する。

### DB・Server Action・依存 — `docs/traps/db.md`

- バックアップにテーブルを足し忘れると誰も気づかない。
- 日本語の全文検索は PGroonga（0015）。
- PostgREST の `.order(col, { referencedTable: X })` は親の行順を変えない。
- ローカルで動いても Vercel で落ちる依存がある。
- `settings` の行が無いと列の既定値は効かない。
- `article_states` の upsert は `onConflict: 'article_id,user_id'`。
- Server Action で throw したエラーの文面は、本番では表に出ない。
- Server Action の失敗は「押しても何も起きない」に化ける。
- supabase-js の問い合わせビルダーは thenable。`async` 関数から返さないこと。
- `head: true` の PostgREST エラーはメッセージが空。
- URL から来る id は形を見てから使う。
- 日時だけでは順番が決まらない。
- 検索は、こちらの都合の外で失敗する。

### 画面と操作 — `docs/traps/ui.md`

- 文字の大きさは目盛り（`@theme`）で動かす。クラスを書き替えない。
- `.prose-rich` の間隔を「直下の子」で書かないこと。
- 「前の記事／次の記事」の帯は、位置が出せないと丸ごと消える。
- 「次の記事」は一覧の1ページ目しか見ていなかった（60件目で止まる）。
- 未読ビューでは「前の記事」に戻れない。来た道はブラウザしか知らない。
- 押し続けると一歩も進まない（← → が「途中で止まる」の正体）。
- ホームバーぶんの余白は、ボタンの padding に足すと「押せない空白」に見える。
- 指の当たり判定は44px。文字ではなく枠に下限を入れる。
- 「まだ何も無い」状態を、実在する値と同じにしないこと。
- 運ぶ列を減らすときは、画面に出ていない使い道を探すこと。

### 速さ — `docs/traps/perf.md`

- 遅さは、まず「ページを描く前」を測ること。
- 走っている途中の先読みは、遷移には使われない（Next 16 のセグメントキャッシュ）。
- 一覧は、画面に出していない列を毎回運んでいた。
- 記事の遷移が遅いのは、`/` を毎回まるごと作り直しているから。
- 埋め込みの入れ子ひとつで7倍遅くなる。
- 並列に投げているものは、いちばん遅い1本しか効かない。
