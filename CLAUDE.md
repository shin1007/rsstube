@AGENTS.md

# RSSTube — プロジェクトの前提

AI要約つきの個人用RSSリーダー。

このファイルには**決まったこと**と**踏んだ罠**だけを書く。以下は別に分けてある:

| 知りたいこと | ファイル |
|---|---|
| いま何がどうなっているか（報告） | `docs/status.md` |
| オーナーがやること（指示） | `docs/owner-todo.md` |
| 無料枠と、詰まったときの見分けかた | `docs/quotas.md` |
| 音声・スライドの他サービス比較 | `docs/tts-options.md` |
| どのサイトの本文が取れるか（110件実測） | `docs/site-compat.md` |
| 設計の全体像 | `docs/plan.md` |
| セットアップと使い方 | `README.md` |

**状態を2か所に書かない。**片方が必ず古くなるので、報告は `status.md` に、
指示は `owner-todo.md` に集めること。

## 決まっていること（蒸し返さない）

- **NotebookLM に公開APIは無い**（2026年8月時点。Enterprise版のみ、コンシューマ版は未提供）。
  なので「アプリで整形 → 人が NotebookLM に投入」が P2 の方針。自前音声は後段（P4、実装済み）。
  2026-07 に「Gemini Notebook」へ改称し、Video Overview / Slide Deck / Infographic なども
  出せるようになったが、**API が無いことは変わっていない**ので自動化の対象にはならない。
  出来上がりは向こうが上なので、書き出しの経路は残し続ける。
  音声・スライドの他サービスの比較は `docs/tts-options.md`。結論は「いまは変えない、
  Gemini TTS の preview が終わったら Google Cloud TTS」。
- **AI は Gemini API の無料枠**を使う。要約 `gemini-3.5-flash-lite`、台本 `gemini-3.5-flash`、
  音声（後段）`gemini-3.1-flash-tts-preview`。いずれも無料枠あり。
- **定期実行は Supabase の `pg_cron`**。Vercel Hobby の cron は1日1回までで
  1時間毎の巡回ができないため。`supabase/scheduler.sql` を参照。
- **Next.js 16.3**。`middleware` は `proxy.ts` に改称、`params`/`searchParams` は Promise、
  Turbopack が既定。訓練データと差異があるので `node_modules/next/dist/docs/` を読むこと（AGENTS.md）。
- **ダークテーマ固定**。朝晩に長時間読む用途のため、切り替えは作らない。
- **記事・要約・フィードは全ユーザー共通**（`0005`/`0006` で実施済み）。
  ユーザーごとなのは `subscriptions` / `article_states` / `folders` /
  `exports` / `digests` / `settings` だけ。ユーザーごとに複製していた頃は
  記事178件＝3.7MB（本文平均8KB）、要約は `BATCH_SIZE = 5` で約36回の Gemini 呼び出しが
  購読者数ぶん掛かる形で、無料枠に収まらなかった。2人目が同じフィードを購読しても
  記事・ジョブは増えず `article_states` だけが増えることを実測で確認済み。
  - `feeds` に書き込みポリシーは無い。購読の入口は `subscribe_feed()` /
    `unsubscribe_feed()` RPC だけ。タイトルや etag の持ち主は巡回であって購読者ではない。
  - 副作用として、ログイン済みなら**全フィードのURLが見える**（誰が購読しているかは見えない）。
    購読 URL 自体を隠したくなったら、そのとき `feeds` の select を絞ること。
  - 要約は共通なので**言語も全体で1つ**しか選べない。当面はオーナーの
    `settings.summary_language` を使う。ユーザーごとに変えるなら
    `summaries` を `(article_id, language)` で持つ必要がある。
- **毎朝ダイジェストの生成時刻はアプリ側で判定する**。pg_cron から毎時叩き、
  各ユーザーの `settings.digest_hour`（日本時間）を**過ぎていて**当日ぶんがまだ
  無ければ作る。「ちょうどその時刻か」で見ないのは、pg_net の失敗やデプロイ中の
  1回で cron が落ちただけでその日のダイジェストが永久に作られなくなるため
  （次の実行はもう別の時刻になる）。過ぎていれば作る形なら自分で追いつく。
  cron の式は1つしか置けないので、ユーザーごとの時刻を式では表せない。
  Vercel Cron を使わないのは1日1回制限に加えて実行時刻が±59分ずれるから
  （6時のダイジェストが7時前にできてしまう）。
- **ダイジェストを作り直すのは「digests の当日行を消してから」**。
  書き出した記事には `exported_at` が付いて選抜から外れるので、`force=1` を
  もう一度叩いても同じものは再現せず、中身の違う2本目ができて1本目が宙に浮く。
  なので `force=1` は生成時刻だけを無視し、当日ぶんの有無は必ず見る。
- **自前音声はセグメント（クリップ）単位で作って保存する**。台本をスライド1枚ぶんに
  区切り、長ければさらに700字ごとに割る。切れ目がそのままスライドの切り替わりに
  なるので、再生側は時刻からスライドを逆算しなくてよい。TTS の1回も短く済み、
  失敗時の再試行が安い。`media_segments` が `idx` と `slide_idx` を別に持つのは、
  1スライドが複数セグメントに割れるため（モデルがスライドを1枚しか返さないことがある）。
- **mp4 は作らない**。スライドは HTML/CSS で描く（plan.md §4）。サーバー側の合成が
  要らず、スマホでも即出る。将来 mp4 が要るなら、同じスライドJSONと
  セグメント音声がそのまま素材になる。
- **ログインの入口は2つ、パスワードとパスキー**（`0034`）。**パスワードを消さない。**
  パスキーは端末に紐づくので、端末を替えたときと生体認証が通らないときに戻る先が要る。
  **Supabase の Auth に WebAuthn は無い**（2026-08 時点）ので、署名の検証は
  `@simplewebauthn/server` でこちらが行い、通ったら Secret キーで
  `generateLink({type:'magiclink'})` を1本作って、その `hashed_token` を自分で
  `verifyOtp` してセッションにする（`lib/auth/passkey-session.ts`。メールは飛ばない）。
  **Cookie を書くので Route Handler から呼ぶこと**——Server Component からだと
  セッションが付かないまま成功が返る。RP ID と origin は環境変数に書かず、
  いま開いている URL から組み立てる（Google の redirect_uri と同じ理由）。
  副作用として**鍵はドメインに紐づく**ので、本番で登録したパスキーは
  プレビューや localhost では使えない。仕様なので直せない。
- **当面は個人用のまま**（2026-08-13 決定）。一般公開は「自分が毎日使えている」状態に
  なってから。費用は無料枠に収まる範囲で設計する。

## 環境

- **このプロジェクトは `C:\dev\rsstube` で開発する。`G:\マイドライブ` 配下には置かない。**
  Google Drive のストリーミングドライブでは `npm install` が `EPERM`→`EBADF` で必ず失敗し、
  `node_modules` を逃がすジャンクションも作れない（Drive の仮想FSが再解析ポイント非対応）。
  同じ install が Drive で10分超＋失敗、`C:\dev` で22秒。
- `G:\マイドライブ\ローカルリポジトリ\RSSTube` に壊れかけの旧コピーが残っている。削除してよい。
- `.env.local` は実値が入っている。Supabase・Gemini とも接続済み。

## いまの状態

**状態と検証状況は `docs/status.md`、やってほしいことは `docs/owner-todo.md`。**
ここに写すと二重管理になって片方が古くなるので、この2つを見ること。

ごく手短にいえば、コードは P0〜P5 まで揃っていて（plan.md に未実装は無い）、
本番は pg_cron で自動運転している。残っているのは実機での確認と、外部サービスの設定。

## 踏んだ罠

- **`res.text()` は日本語のサイトで文字化けする。** Content-Type に charset が無いと
  UTF-8 として読むが、自治体・省庁には Shift_JIS / EUC-JP が現役で残っていて、
  charset を meta にだけ書いていることがある（例: `mhlw.go.jp`）。
  厄介なのは**化けても長さはあるので抽出は「成功」に見える**こと。化けた文字列が
  そのまま要約に回る。`lib/feeds/charset.ts` で Content-Type → BOM → meta の順に見る。
- **バックアップにテーブルを足し忘れると誰も気づかない。** `scripts/db-tables.mjs` が
  一覧で、0005 で切り出した `subscriptions` が長らく漏れていた（復元しても購読ゼロ）。
  jsonb 列は JSON 文字列にしてから戻すこと。JS の配列のまま渡すと pg が
  Postgres の配列に直してしまい `invalid input syntax for type json` になる
  （`text[]` の列は配列のままで正しいので、値の形では区別できない。列の型を DB に聞く）。
- **日本語の全文検索は PGroonga（0015）。** `simple` 辞書の tsvector は空白で区切るだけで
  日本語の文が丸ごと1語になり、0001 で張ったまま一度も使えていなかった。PGroonga は
  Supabase で `create extension` できる。**索引を張るだけで既存の `ilike` が8.2倍速**に
  なる（PGroonga が `ilike` も肩代わりするため、クエリの書き換えは要らない）。
  `&@~` を使えばさらに25倍だが、PostgREST から演算子を呼べないので RPC が要る。
  trgm と simple の索引は落とした（プランナが選ばなくなったため）。
- **PostgREST の `.order(col, { referencedTable: X })` は親の行順を変えない。**
  埋め込んだ X 側の並びを変えるだけ。`articles` を主に `summaries` を埋め込んで
  重要度順にしていたつもりが、実際は published_at 順のままだった（`0007` で発覚）。
  並べ替えは必ず親テーブルの列で行うこと。`articles.importance` はそのための複製。
- **ローカルで動いても Vercel で落ちる依存がある。** `jsdom@30` が引く
  html-encoding-sniffer が CJS のまま ESM を require していて、dev では通るが
  Vercel の外部モジュール読み込みで `ERR_REQUIRE_ESM`。本文抽出は `linkedom` に変えた。
  依存を足したら本番でも1回叩いて確かめること。
- **`settings` の行が無いと列の既定値は効かない。** 設定画面を一度も保存していないと
  行そのものが無いので、`notebooklm_prompt` の default（0001 に書いてある文面）は
  出てこない。空文字のまま書き出すと、NotebookLM が素の読み上げになる。
  既定値は `lib/export/prompt.ts` に TS 側の定数としても置き、行が無いときはそちらを使う。
  文面を変えるときは 0001 の default と両方を揃えること。
- **`article_states` の upsert は `onConflict: 'article_id,user_id'`。** 0005 で
  主キーが (article_id, user_id) に変わったのに `article_id` だけを指定したままの
  箇所が残っていた（書き出しの送信済みマーク）。1人で使っている間は表に出ないが、
  2人目が同じ記事を書き出した瞬間に他人の行へ当たって弾かれる。
- **`responseSchema` の `maxItems` は「項目3つまで」なら使える。** 7つの
  オブジェクトに付けると `INVALID_ARGUMENT` で弾かれる（理由が本文に出ないので
  原因不明の400に見える）。**そして上限はプロンプトでは守られない。**
  スライドの項目が7つで `maxItems` を諦めていたとき、モデルが同じスライドを
  延々と繰り返して出力トークンを使い切り（実測23302トークン・73639字）、
  **記事1本からの音声生成は必ず失敗していた**。ダイジェスト（8件）では起きないので
  気づきにくい。いまはスライドを `{type, heading, body[]}` の3項目に平たくして
  `maxItems` を効かせ、受け取ってから型ごとの形に戻している（`lib/ai/script.ts`）。
  項目を増やしたくなったら、この壊れ方が戻ることを思い出すこと。
- **`maxOutputTokens` は思考トークンも含む。** 3.5-flash は答える前に考えるので、
  8192 にしたら思考だけで使い切って本文が716字で切れた。絞るときは
  `thinkingBudget` も一緒に指定する。`finishReason === 'MAX_TOKENS'` を見て
  「打ち切られた」と分かるようにしてある（見ないと JSON の parse エラーとして
  出てきて原因が分からない）。
- **`gemini-3.5-flash` の無料枠は1日20リクエスト。** 台本生成1本で1回使う。
  デバッグで何度も叩くとその日ぶんが尽きる（実際に尽くした）。
  使用量は設定画面の表で見られる。要約の flash-lite とは別枠。
  **無料枠と上限の全体は `docs/quotas.md` にまとめてある。**公開情報だと
  Flash 系は500〜1500 RPD と書かれていることが多いが、手元のキーは20だった。
  外の数字ではなく `ai_usage` と 429 の本文（`limit:` が書いてある）を見ること。
- **1回のワーカー実行は時間で切る。** 件数だけで絞っても、TTS は1セグメントに
  数十秒かかることがあり `maxDuration = 60` を超えると関数ごと落ちて、
  running のジョブが宙に浮く。`TIME_BUDGET_MS` を見ながら次の種類に進む前に
  切り上げる形にしてある。
- **VAPID 鍵を作り直すと登録済みの端末には二度と届かない。** 購読はブラウザ側で
  公開鍵に紐づくので、鍵を替えたら `push_subscriptions` を空にして端末ごとに
  登録し直すしかない。本番と手元で鍵を揃えること（別々に生成すると、
  手元で登録した端末には本番からの通知が届かない）。
- **本文が取れないサイトに、こちら側の直しどころは無い……が、3つに分けて数えること。**
  110件の実測（`docs/site-compat.md`）で失敗したのは9件、すべて 403/401/429 だった。
  ブラウザと同じ User-Agent にしても結果は変わらないので、UA ではなく
  IP か TLS 指紋で弾かれている。**UA を偽装しても意味が無い**ので、
  正直な UA を名乗り続けること。
  ただし**「サイト単位で読めるか」と「記事単位で取れたか」は別物**だった。
  2026-08-26 に本番の失敗567件を叩き直したら、3種類に割れた:
  - **本文がHTMLに無い**（東洋経済347件・全体の61%）。200 は返るが Readability が
    掴むのはグローバルメニュー。0016/0018 の使い回し判定が正しく弾いている。直せない
  - **弾かれている**（Hacker News185件・33%）。リンク先が第三者のサイトで 403/401、
    または JS でしか描かれない。直せない
  - **一度失敗しただけ**（Aeon21件ほか・6%）。**いま叩けば2万字の本文が返る。**
    `extracted_at` が入ると「処理済み」になり、二度と取りに行かない作りだった
  3つ目のために `0028` で理由（`extract_fail`）を残し、`network` と `short` のときだけ
  6時間後に1回だけ取り直す。**`recycled` と `blocked` は取り直さない**——
  東洋経済と Hacker News で毎日20件ぶん、何も変わらない取得を繰り返すことになる。
- **フィード単位の「読めなさ」はどの数字にも出ない。** 取得は 200 で成功し続け、
  新着も毎日来るので `error_count` も `last_article_at` も正常なまま、要約だけが
  RSS の抜粋から作られて薄くなる。東洋経済は427件中347件（81%）がこの状態で、
  設定画面には何も出ていなかった。`feed_content_stats()`（`0028`）で数え、
  `lib/feeds/health.ts` が半分を超えたフィードを「本文が取れない」として出す。
  **割合だけで判定しないこと。** 件数の下限（10件）が無いと、購読したての
  フィードがほぼ必ず引っかかる。
- **`jobs` にはRLSポリシーが1つも無い。** 0005 で `jobs_owner` を落として以来、
  ジョブを積めるのは Secret キーだけ（ユーザーに他人のジョブを見せない・作らせない
  ため、意図してそうしてある）。ログイン中のユーザーのクライアントで
  `enqueue()` すると 42501 で必ず弾かれる。`requestMedia()` がこれをやっていて、
  **「音声にする」は media の行だけ作って script ジョブを積まずに落ちていた**。
  しかも一意索引のせいで押し直すと「既に作ってあります」になり、永久に queued の
  行が残る。ジョブを積むのは `createAdminClient()` から。
- **Server Action で throw したエラーの文面は、本番では表に出ない。** Next が
  digest に置き換えるので、クライアントに届くのは `Minified React error #441`
  （中身を伏せた Server Components のエラー）だけ。`throw new Error('記事が見つかりません')`
  のような**見せるつもりの文面は値として返す**こと。actions/ には throw のまま
  残っている箇所がまだある（feeds / exports / push / drive）。
- **スライドは文字だけ、と気づかれない。** 中身（見出し・箇条書き）は正しく出て
  いても、罫線も色も番号も絵も無いと「スライドが出ていない」と読まれる（実際に
  そう報告された）。0025 で記事の `image_url`（og:image / RSS の enclosure）と
  `media.cover_path` を足し、表紙の絵と差し色・枚数表示を入れてある。
  **画像URLを直接 `<img>` に貼らないこと。** 相手のサイトに毎回取りに行くことに
  なるし、記事が消えると絵だけ先に欠ける（音声は14日残る）。Storage へ写す
  （`lib/media/cover.ts`）。写すのは media 1本につき1枚だけ——全記事ぶん溜めると
  Storage の無料枠(1GB)を音声と奪い合う。
- **後から全記事を浚う処理を足さない。** `image_url` を後付けするとき、既存の
  未設定が2024件あった。表紙が要るのは音声にする1本だけなので、要るときに
  先頭3件まで取りに行って記事側へ書き戻す形にしてある（`resolveCoverUrl`）。
  同じ判断は他の後付け列にも効くはず。
- **Server Action の失敗は「押しても何も起きない」に化ける。** Next は投げられた
  エラーを digest に置き換えるので、本番で届くのは `Minified React error #441`
  だけ。`throw new Error('フィードを読めませんでした')` の文面は一度も表に出ない。
  設定の保存ボタンでは、成功しても画面が1ドットも変わらないため、**成功・失敗・
  未送信の3つが全部同じに見えていた**。
  - 見せたい文面は**値として返す**こと。`lib/actions/result.ts` の `attempt()` で
    包めば、中の `throw` をそのまま文面にできる（中の書き方は変えなくてよい）。
  - `attempt()` は `unstable_rethrow()` を通すこと。**`redirect()` と `notFound()` は
    throw で動く**ので、飲み込むと画面遷移が「失敗しました」という文字列に化ける。
  - 素の `<form action={サーバー関数}>` は失敗を出す場所が無い。`ActionForm` を使う。
  - 成功にも手応えを出す。変化が見えない操作（保存・受付）は、文言と時刻を出さないと
    押せたかどうか分からない。
- **引いたジョブを全部処理しないなら、その場で戻すこと。** `claim_jobs` は返す前に
  `running` にして `attempts` を +1 する。ワーカーは `TIME_BUDGET_MS` を見ながら
  回るので**引いた数と処理した数は一致しない**（TTS は1回で1〜2件しか進まない）。
  手を付けなかったぶんは 0004 の拾い直し（15分）を待つことになり、
  「2件進んでは15分待つ」で完成が異常に遅くなる。しかも拾い直しのたびに
  `attempts` が増えるので、**一度も実行されていないジョブが5回の空回りで
  failed に落ちる**（実際に tts が5件この状態になっていた）。
  `release_jobs`（0026）で即座に戻す。時間切れは失敗ではないので attempts も戻す。
- **待ち時間の見当は件数の上限からではなく実測から出す。** `TTS_PER_RUN` は4だが、
  時間予算のほうが先に来るので実際は1〜2件。上限を使うと出した時刻を過ぎても
  終わらず、何も出さないより悪い（`lib/media/eta.ts`）。
- **ジョブが諦めたら、それを待っている行も誰かが落とすこと。** `markFailed` は
  429・503 で `media.status` を意図して動かさない（1セグメント転んだだけで全体を
  殺さないため）。正しいが、**ジョブが `max_attempts` で failed に落ちたあとに
  media を落とす人がどこにもいなかった。**結果、`synthesizing` のまま8日残った1本が、
  `/listen` では「合成中」＋できあがり予定時刻を永久に出し続け、failed ではないので
  再試行の導線も出ず、一意索引のせいで「音声にする」を押し直しても
  「既に作ってあります」しか返らなかった（＝**手の出しようが無い**）。
  `0027` の `fail_abandoned_media()` が「生きたジョブが1つも無い」を死亡判定にして落とす。
  **経過時間で判定しないこと。** `fail_job` のバックオフは最大12時間あるので、
  まだ生きているものを殺す。
- **作り直しは、できている途中まで残して再開する。** 台本は1日20回、合成もセグメント
  ごとに無料枠を食う。全部やり直すと、詰まった原因が枠切れだったときに同じ枠切れを
  もう一度踏む。`retry_media()`（`0027`）は諦めたジョブの `status` を戻すだけで、
  payload を作り直さない。`jobs_pending_unique_idx` は `payload ->> 'article_id'` を
  見るので、`article_id` を持たない script / tts の重複はDBでは防げない。
  積む前に「生きたジョブがあるか」を自分で見ること。
- **文字の大きさは目盛り（`@theme`）で動かす。クラスを書き替えない。** 「全体的に
  2段階大きく」で `text-xs`（165か所）と `text-sm`（91か所）を1つずつ直すと、必ず
  塗り残しが出る。`--text-xs` などを再定義すれば既存のクラスのまま全部が上がる。
  **`body` にも実寸を入れること**——rem は `:root` 基準なので、クラスを付けていない
  文字だけ 16px で取り残される。**そして実寸を直書きした箇所を探し直すこと。**
  スマホの入力欄には iOS の自動ズーム避けで `font-size: 16px` が入っていて、
  目盛りだけ上げると **text-base（20px）の入力欄がスマホでだけ 16px に縮んだ**
  ——大きくするための変更が、いちばん打ちにくい画面でだけ効かない形になる
  （`max(16px, 1em)` の下限に変えた）。`.prose-rich` の見出しも rem 直書きで、
  放っておくと見出しが本文より小さくなる。**JSX の `text-[15px]` も同じ**——
  記事本文がこれで、一番長く見る文字だけが元の大きさで取り残されていた（23か所あった）。
  そして**入るはずだったものが入らなくなる。** 記事の上の帯は6つのボタンで1行ぎりぎりで、
  溢れた瞬間に flex が縮めにきて **`音声にする` が1文字ずつ縦に折り返り、帯が画面の
  1/4を占めた**。日本語のラベルは1文字でも折り返せるので、横スクロールにもならずに
  黙って縦に伸びる。**並ぶボタンには `whitespace-nowrap` と `shrink-0` を付け、
  狭い画面用の短い形を持たせること**（読み上げには `aria-label` で長いほうを渡す）。
- **`.prose-rich` の間隔を「直下の子」で書かないこと。** `> * + *` で段落の間隔を
  付けていたが、**一度も当たっていなかった**。抽出した本文は
  `<div><div><p>…</p><p>…</p></div></div>` のように包まれていて、直下の子は div 
  ひとつだけ。実データ10本で当たった要素は全部で0個、段落は13〜111個あった。
  Tailwind の preflight が `<p>` の既定マージンも消すので、**段落の間隔はゼロ**の
  まま行間1.9だけで文が続き、改行と段落の切れ目が見分けられなかった。
  深さを問わず要素名で当てること。**下マージンだけにする**——上マージンだと
  先頭の段落のぶんが包みの div をすり抜けて本文の上に隙間を作る（マージンの相殺）。
  1字下げ（`text-indent`）は入れない。日本語では字下げと段落間の空きはどちらか
  片方にするもので、スクロールして読む画面なら空きのほうが切れ目を見つけやすい。
- **巡回は `feeds` を直に見てはいけない（`0029`）。** feeds は全ユーザー共通で
  購読の有無を持たない（0005）ので、直に select すると**購読をやめたフィードも
  毎時取りに行き続ける**。記事は入り続け、本文抽出も要約（Gemini の無料枠）も走るのに、
  画面には出ないので表からは分からない。掃除（`purge_orphan_feeds`）任せにもできない
  ——あれはスターや書き出し済みの記事が1件でも残っているフィードを消さないので
  （消すと記事ごと巻き添えになる）、印を付けた記事があるフィードは解除後も
  永久に残り、永久に巡回される。18本を外したとき、11本がこれに当たった。
  `feeds_to_poll()` を使うこと。
- **`articles.url_hash` は全体で一意。** フィードごとではない。同じ記事URLを配る
  フィードを2本登録すると、**後から購読したほうには過去記事が1件も入らない**
  （insert が conflict で落ちるだけなので、エラーにもならず「新しいフィードが空」に
  見える）。実例: `nazology.net/feed` と `nazology.kusuguru.co.jp/feed` は
  別ホストだが中身は同じで、記事リンクはどちらも kusuguru を指す。
  乗り換えるときは `articles.feed_id` を新しい行へ移すこと。
- **`JST` は JavaScript が解釈できない。** `new Date('Wed, 26 Aug 2026 17:00:00 JST')` は
  `Invalid Date`。RFC 822 が名前で認めているのは UT / GMT と北米の4つ（EST・CST・
  PDT など。**これらは V8 がそのまま読む**）だけで、`JST` `KST` `MSK` `CEST` は入っていない。
  rss-parser も `new Date()` に任せているので `isoDate` が付かない。
  **静かに壊れる**——取り込みも本文も要約も普通に通り、`published_at` だけが null になる。
  一覧は `nulls last` なので**その記事が全部末尾に沈む**（千葉県の103件＝全体の29%が
  この状態だった。「新着なのに一番下」としか見えない）。`lib/feeds/date.ts` で
  名前のタイムゾーンを読み替える。**表に足すのは「世界で1つに決まる略号」だけ**——
  `CST`（北米中部/中国）や `IST`（印/以/愛）を入れると、静かに違う時刻が入る。
- **要約に「本文が取れなかった」ことを書かせない。** プロンプトに
  「(注: 本文が取得できず、RSSの抜粋のみ)」とだけ渡していたら、モデルが素材の
  乏しさのほうを要約してきた（実データで30件が「本文が取得できずRSSの抜粋のみのため、
  詳細な内容を確認することはできない。」だけになっていた）。**漫画のフィードは本文が
  無いのが当たり前**なので全話がこれで埋まり、しかも1件ぶんの無料枠を使う。
  読み手が知りたいのは記事の中身であって、こちらの取得結果ではない。
- **直したら、それが既存の行にも届くか確かめること。** 取り込みは `ignoreDuplicates`
  なので、`JST` を読めるようにしても**既に入っている記事の日付は埋まらない**。
  そのたびに手で UPDATE を流すのでは、気づいた人がいるときしか直らない。
  `backfill_published_at()`（`0030`）を取り込みの経路に置いて、その回の取得に
  入っていた項目ぶんだけ埋め戻す。全記事を浚うわけではないので
  「後から全記事を浚う処理を足さない」には反しない。次に別の略号へ対応したときも、
  次の巡回で勝手に追いつく。
- **先回りで塞げないものは、欠けたことが見えるようにする。** タイムゾーンの略号を
  全部表に入れることはできない（`MSK` `CEST` …）。`feed_content_stats()` が
  日付なしの件数も数え、8割を超えたフィードを健康診断に「日付が無い」として出す。
  閾値を半分にしないのは、日付を打たない項目が混ざるフィードが普通にあるため。
- **消毒で枠を外した子は、もう一度見直すこと。** 走査は `Array.from(node.children)` の
  写しを回すので、知らないタグを `replaceWith(...childNodes)` で外すと、
  **その中身はその回では一度も訪れない**。属性が絞られず、相対URLも直らない。
  見つかったきっかけは画像だった——厚労省・PLOS などで `<img src="/content/…">` が
  相対のまま保存され、ブラウザがこちらのドメインを見に行って404（39記事）。
  同じ記事でも直接の子だった img は絶対URLに直っている、という混ざり方をする。
  **危ないのは属性のほう。**知らないタグの中に `<img onerror=…>` を置かれると
  その on* が残る。実データに混入は無かったが、それは入力の運。
  外したぶんがあれば同じ節をもう一度walkする（`0026` ではなく `sanitize.ts`）。
- **記事の遷移が遅いのは、`/` を毎回まるごと作り直しているから。** 記事は
  `?article=` の付け替えで開くので、1回の遷移がサイドバー＋一覧60件＋本文の
  再構築になる。実測で RSC 89KB（転送26KB）・Supabase に6往復。中身のうち
  変わっているのは本文だけで、**運んでいるものの大半は一覧のほう**（要約の箇条書きが
  日本語で入るため。本文HTMLは平均21KBだが、記事1本ぶんの重さより一覧60件が重い）。
  DBは速い（実測 1〜17ms）ので、遅さは往復と量から来ている。ここに3つ重なっていた:
  - **`revalidatePath('/')` は「ページ全体を描き直した RSC」を Server Action の
    応答に積んで返す**（呼ばなければ描き直さない。`action-handler.js` の
    `skipPageRendering`）。開いた拍子の既読がこれを連れていたので、
    **1タップにつき遷移と同じ重さの描き直しがもう1回**走っていた。
    見せる変化がある操作（スター・あとで・手の既読）だけ revalidate する。
  - `MarkReadOnView` が同じ記事にもう1通 markRead を投げていた。開くのと
    既読の書き込みは同時に走るので、**本文を描く読み取りが勝つと `is_read` は
    まだ false** で返り、二度目が止まらない。`lib/read-marks.ts` に送った id を
    覚えて止める。既読の見た目もそこから一覧へ配る（revalidate をやめたので、
    サーバーの描き直しには頼れない）。
  - **`router.prefetch(href)` は動的ルートでは何もしない。**既定は `kind:'auto'`
    で、`loading.js` の境界までしか取らない＝`/` には境界が無いので**要求が
    1本も飛ばない**（3秒待って通信ゼロ。効いているつもりで何も起きない）。
    `kind:'full'` を渡すこと（`lib/prefetch.ts`）。`<Link prefetch>` も同じ意味。
    先読みしたぶんは静的扱いで5分残るので、「次の記事」は**往復ゼロ**で切り替わる。
    先読みは行きそうな1件だけにする——1件ごとにサーバーがページを丸ごと組む。
    **`revalidatePath` は先読みも一緒に捨てる**ので、読み進める経路で revalidate を
    呼ぶと待ち時間がそのまま戻ってくる。
  副作用として、サイドバーの未読数は開いた記事1件ぶん遅れて減る（次の描画で追いつく）。
- **HTMLに本文が無いページは、本体が別にある（PDF・別ページ）。** 官公庁と自治体に多い形で、
  表題とリンクだけのHTMLに中身が付いていない。厚労省の「○○医療費の動向」がこれで、
  本体のPDFは35ページ・54,605字あった。`lib/feeds/followup.ts` が1段だけ辿り、
  PDFは `unpdf` で読む（`lib/feeds/pdf.ts`）。記事URLが直接PDFのときも読む
  （以前は「HTMLではない」で捨てていた。Hacker News のPDFリンクもこれに当たる）。
  ここで踏んだものを順に:
  - **失敗ではなく「成功」に見えていた。** 入口ページでは Readability が掴むものが無く、
    代わりに**フッターを本文として返す**。実例で返ってきたのは
    「〒100-8916 …Copyright ©…」の118字で、`MIN_CONTENT_CHARS`（100）を超えているので
    抽出成功として保存され、`feed_content_stats()` にも出てこない。
    なので本体を疑う境目は別に持つ（`FOLLOW_BELOW_CHARS` = 600）。
  - **枠の判定に body の class を混ぜない。** 厚労省は `<body class="t-mhlw nav03">`。
    `nav` に当たるので、素直に祖先を遡ると**そのページのリンクが1本残らず枠扱い**になり、
    候補ゼロで静かに何もしなくなる。BODY と HTML は見ないこと。
  - **辿った先が本体でないほうが怖い。** 「取れなかった」は見れば分かるが、
    「関連ページの本文がこの記事として要約されている」は読んでも気づけない。
    臆病に倒す: 別サイトは追わない / 候補が4つ以上あるページ（一覧・目次）は何も追わない /
    `…/` や `index.html`（節の入口）は本体にしない——千葉県の「協議会の開催について」から
    一段上を読み、その回の案内ではなく協議会そのものの説明を本文にしていた /
    **辿った先が今より200字以上増えないなら辿らなかったことにする**——漫画のページで
    「最新話から」を辿ったら、行った先も同じ194字の作品紹介だった。
  - **審議会のページは先頭2本が議事次第と委員名簿。** 順番どおりに3本読むと
    「会議の次第と出席者」だけが要約される。手続き書類は後回しにして資料を先に読む。
  - **「本体はこちら」の1行はHTMLにだけ置く。** 要約に渡すテキストに入れると、
    モデルが記事の中身ではなく取得の事情を要約してくる（前科あり）。
  - 既にある記事は取り直さない（`extracted_at` があるため）。手で流すのは
    `scripts/reextract-thin.mjs`。
- **「前の記事／次の記事」の帯は、位置が出せないと丸ごと消える。** 出すかどうかは
  `prevHref || nextHref` で決めていて、**一覧に居ない記事は位置が出せない**。
  未読ビューでこれが常に起きていた——記事を開くと既読になり、同じ描画で引いた
  一覧からはもう抜けているため（`index === -1`）。PCは一覧が横に見えているので
  気づきにくいが、**スマホには一覧へ戻る以外の導線が無い**ので、1本目を開いた
  瞬間から読み進める手が無くなる。
  以前は「未読ビューでは引いても空振りだから」と、位置の引き直しごと飛ばしていた。
  空振りなのは事実だが、**空振りしたときに何を出すかを決めていなかった**のが穴。
  いまは id で見つからなければ `published_at` で「居たはずの場所」を出す
  （`app/page.tsx`）。並びが importance のときは日付では出せないので、そこは諦める。
