/**
 * サーバー側で日時を出すときの時間帯。
 *
 * **`toLocaleString('ja-JP')` に時間帯を渡さないと、動かしている機械の設定で
 * 決まる。** 手元は日本時間なので正しく見えるが、**Vercel は UTC** なので、
 * サーバーで組み立てた文字だけ9時間ずれる。「取得 12:07」が本番では
 * 「取得 3:07」になる形で、画面を見ても壊れているとは分からない
 * （それらしい時刻が出るので）。
 *
 * **`'use client'` のファイルでも渡すこと。** ここには一度
 * 「クライアントで出すぶんは読む人の時計でよい」と書いてあったが、**誤り**だった。
 * クライアント部品でも**最初の1枚はサーバーで描かれる**ので、渡さないと
 * サーバー（UTC）とブラウザ（日本時間）で別の文字になり、React が hydration の
 * 食い違いとして弾く——本番のコンソールに出続けていた `#418` がこれ。
 * 手元で `next start` するとサーバーも日本時間なので**一致してしまい再現しない**
 * （`TZ=UTC npm start` で本番と同じ条件になる）。
 *
 * 見つけたのは Cache Components に移したとき。時間帯を渡していない
 * `toLocaleString` は prerender では答えが決まらないので、Next が
 * 「同期IO」として弾いてくれた（それまでは静かにずれていた）。
 */
export const JST = 'Asia/Tokyo';
