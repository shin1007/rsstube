/**
 * NotebookLM の「音声概要をカスタマイズ」欄に貼る指示文の既定値。
 *
 * settings テーブルの列既定値（0001）と同じ文面をここにも置いてある。
 * 設定を一度も保存していないと settings の行自体が無く、DB の既定値は効かない
 * （行が無ければ列の既定値も出てこない）。音声の出来はこの指示文でほぼ決まるので、
 * 空のまま書き出してしまうと毎朝のダイジェストが素の読み上げになる。
 *
 * 文面を変えるときは 0001 の default と揃えること。ズレても壊れはしないが、
 * 「設定画面に出ている文面」と「実際に使われる文面」が食い違う。
 */
export const DEFAULT_NOTEBOOKLM_PROMPT =
  '日本語で話してください。専門外の聴き手に向けて、各記事について「何が新しいのか」「なぜ重要か」を中心に、10分程度で解説してください。';
